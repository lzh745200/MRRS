name: Nightly Full Test Suite

on:
  schedule:
    - cron: '0 2 * * *'  # 每天凌晨 2:00 UTC
  workflow_dispatch:

permissions:
  contents: read

jobs:
  backend-full:
    runs-on: ubuntu-latest
    timeout-minutes: 90
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4
      - uses: actions/setup-python@a26af69be951a213d495a4c3e4e4022e16d87065 # v5
        with: { python-version: '3.11' }
      - run: pip install -r backend/requirements.txt -r backend/requirements-dev.txt
      - name: Full test suite with coverage
        run: |
          cd backend
          # 2026-08-24：改用 xdist 并行（对齐 PR Checks 已验证配方），
          # 全量 1.2 万用例 + 98% 覆盖率门禁约 15-20 分钟完成；
          # 单进程模式需 60-90 分钟，曾三次被外部协调取消（runs #10/#11/#12）。
          python -m pytest tests/ -q --tb=line \
            -n auto \
            --cov=app --cov-report=html --cov-report=json --cov-report=xml \
            --cov-fail-under=98 \
            --reruns 2 --reruns-delay 1 \
            --durations=20 \
            --junitxml=test-results.xml
      - name: Upload test results
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4
        if: always()
        with:
          name: backend-test-results
          path: backend/test-results.xml
      - name: Upload coverage
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4
        if: always()
        with:
          name: backend-coverage-html
          path: backend/htmlcov/
      - uses: codecov/codecov-action@b9fd7d16f6d7d1b5d2bec1a2887e65ceed900238 # v4
        with:
          directory: backend
          flags: backend-nightly
        continue-on-error: true

  frontend-full:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4
      - uses: actions/setup-node@a0853c24544627f65ddf259abe73b1d18a591444 # v5
        with: { node-version: '22' }
      # npm ci 严格按 lockfile 安装（与 pr-checks 一致），保证 nightly 与 PR 结果可复现
      - run: cd frontend && npm ci --legacy-peer-deps
      - name: Full test + coverage
        run: cd frontend && npx vitest run --coverage --reporter=junit --outputFile=test-results.xml 2>&1
      - name: Upload test results
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4
        if: always()
        with:
          name: frontend-test-results
          path: frontend/test-results.xml
      - name: Upload coverage
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4
        if: always()
        with:
          name: frontend-coverage
          path: frontend/coverage/
      - uses: codecov/codecov-action@b9fd7d16f6d7d1b5d2bec1a2887e65ceed900238 # v4
        with:
          directory: frontend
          flags: frontend-nightly
        continue-on-error: true

  quality-report:
    runs-on: ubuntu-latest
    needs: [backend-full, frontend-full]
    if: always()
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4
      - name: Generate quality summary
        run: |
          echo "## 每日质量报告 ($(date +%Y-%m-%d))" > quality-report.md
          echo "" >> quality-report.md
          echo "| 检查项 | 状态 |" >> quality-report.md
          echo "|--------|------|" >> quality-report.md
          echo "| 后端测试 | ${{ needs.backend-full.result == 'success' && '✅' || '❌' }} |" >> quality-report.md
          echo "| 前端测试 | ${{ needs.frontend-full.result == 'success' && '✅' || '❌' }} |" >> quality-report.md
      - name: Upload quality report
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4
        with:
          name: quality-report
          path: quality-report.md
