# Assistance Management Information Management System - Production Dependencies
# 安装: pip install -r requirements.txt
# 开发依赖: pip install -r requirements-dev.txt

# Web 框架
fastapi==0.136.3
starlette==1.3.1
uvicorn[standard]==0.48.0
python-multipart==0.0.31
slowapi==0.1.9

# 数据库
sqlalchemy==2.0.50
alembic==1.13.1

# 数据验证
pydantic==2.13.4
pydantic-settings==2.14.2
email-validator==2.1.1

# 安全与认证
PyJWT==2.9.0
passlib[bcrypt]==1.7.4
bcrypt==4.0.1

# 工具库
python-dotenv==1.2.2
httpx==0.28.1
diskcache==5.6.3
psutil>=5.10.0
python-dateutil==2.9.0.post0
pytz==2024.1
filelock==3.13.1

# 日志
structlog==25.5.0
python-json-logger==2.0.7

# 数据处理与导出
openpyxl==3.1.5
xlsxwriter==3.2.9
pandas==3.0.3
python-docx==1.2.0
reportlab==4.1.0
mammoth==1.6.0

# 文件上传安全（MIME 检测 + 图片净化）
# Windows: python-magic-bin 自带 libmagic DLL
# Linux/麒麟: 使用 python-magic + 系统 libmagic1 (apt install libmagic1)
python-magic-bin==0.4.14; sys_platform == 'win32'
python-magic==0.4.27; sys_platform != 'win32'
Pillow==12.2.0

# 任务调度（定时备份）
# APScheduler 已移除（使用 threading.Timer 替代，见 backup_scheduler.py）

# Redis支持（可选，用于性能优化）

# 安全增强
pyotp==2.9.0
qrcode==7.4.2
cryptography==48.0.0

# AI功能增强
scikit-learn==1.4.2
prophet==1.3.0
snownlp==0.12.3

# 舆情监控
scrapy==2.11.1
feedparser==6.0.11
