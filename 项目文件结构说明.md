# 帮扶管理信息系统 — 文件结构说明（小白友好版）

> **版本**: v1.8.1 | **更新**: 2026-08-13
> 英文名：Assistance Management Information System
> 📖 本文档面向所有开发者（包括第一次接触本项目的同学），用最通俗的语言解释每个文件夹和文件的作用。

---

## 目录

- [这个系统是干什么的？](#这个系统是干什么的)
- [什么是前后端分离？](#什么是前后端分离)
- [系统技术栈简介](#系统技术栈简介)
- [常见文件类型说明](#常见文件类型说明)
- [如何找到我需要的文件？](#如何找到我需要的文件)
- [项目根目录总览](#项目根目录总览)
- [backend/ — 后端（服务器端）](#backend--后端服务器端)
- [frontend/ — 前端（用户界面）](#frontend--前端用户界面)
- [electron/ — 桌面应用外壳](#electron--桌面应用外壳)
- [其他重要目录](#其他重要目录)
- [CI/CD 自动化流程](#cicd-自动化流程)
- [Pre-commit 工作流](#pre-commit-工作流)
- [开发约定](#开发约定)
- [版本历史](#版本历史)

---

## 这个系统是干什么的？

> 🎯 **一句话介绍：** 这是一套帮助帮扶干部管理乡村振兴帮扶工作的**单机桌面软件**——装在电脑上就能用，**不需要联网**，所有数据都存在你自己的电脑里。

### 它管理什么？

| 管理对象 | 通俗说明 | 对应菜单位置 |
|---------|---------|-------------|
| 🏘️ **帮扶村** | 每个村的基本情况（人口、收入、产业、基础设施等 10 个板块），按年度记录和对比 | 帮扶村管理 |
| 🏫 **帮扶学校** | 结对帮扶的学校信息、受助学生、帮扶项目 | 帮扶学校 |
| 📋 **帮扶项目** | 修路、引水、产业扶持等项目的立项、进度、里程碑、附件 | 项目管理 |
| 💰 **帮扶经费** | 每一笔钱从申请 → 审批 → 拨付 → 使用 → 报销 → 决算 → 归档的全过程 | 经费管理 |
| 📜 **帮扶政策** | 政策文件的发布、分类、搜索、收藏，可直接预览 | 帮扶政策 |
| 🏢 **组织机构** | 帮扶单位的层级树（省/市/县/乡镇） | 组织管理 |
| ✅ **审批** | 经费申请等事项的审批流转（待审批→审批中→已通过/已驳回） | 审批管理 |
| 📊 **数据分析** | 把数据变成图表：年度指标对比、经费使用分析、项目进度分析、帮扶成效 | 数据分析 |

### 它有什么特点？

- **完全离线**：不用连网，断网也能正常工作（AI 等个别增强功能除外）
- **数据在本机**：所有数据存在一个 SQLite 数据库文件里（`data/` 目录），软件自带备份/恢复功能，可备份到 U 盘
- **多机协同**：多台电脑之间可以通过加密数据包互相同步数据
- **四种角色**：`super_admin`（超级管理员）/ `admin`（管理员）/ `user`（普通用户）/ `viewer`（只读查看），不同角色看到和操作的按钮不同
- **两种安装包**：Windows 10/11（x64）安装程序 和 国产麒麟 V10（ARM64）deb 包

### 谁应该读这份文档？

| 你是… | 建议 |
|-------|------|
| 只想**使用**系统的人 | 不用读本文，去看 `docs/02-用户手册/使用指南.md` |
| 第一次**接手开发**的同学 | 从本文开始，按目录顺序读一遍，再用"如何找到我需要的文件"一节速查 |
| 要**部署安装**的运维 | 看 `docs/04-部署文档/`，本文的"构建与部署"一节作参考 |

---

## 什么是前后端分离？

🏠 **打个比方：** 把整个系统想象成一家餐厅。

| 角色 | 对应系统部分 | 做什么 |
|------|-------------|--------|
| 🍽️ 前厅（顾客看到的） | **前端**（`frontend/`） | 用户看到的界面——按钮、表格、表单、页面。用 Vue 3 编写，运行在浏览器或 Electron 桌面窗口中 |
| 👨‍🍳 后厨（顾客看不到的） | **后端**（`backend/`） | 处理数据——保存、查询、计算、验证。用 Python FastAPI 编写，运行在服务器上 |
| 📋 菜单（沟通桥梁） | **API 接口** | 前端通过 API（网址形式的请求）向后端"点菜"，后端"做好菜"返回数据 |
| 🗄️ 仓库 | **数据库**（SQLite） | 所有数据最终存放在数据库文件里（`data/` 目录下的 `.db` 文件） |
| 📦 外卖包装盒 | **Electron**（`electron/`） | 把网页"打包"成一个可以双击打开的桌面程序（.exe） |

**前后端分离** = 前厅和后厨各干各的，通过菜单（API）沟通。好处是：换前厅装修（改界面）不用动后厨（改后端），反过来也一样。

```
用户操作 → 前端页面(Vue) → 发送API请求 → 后端处理(FastAPI) → 读写数据库(SQLite) → 返回结果 → 前端显示
```

---

## 系统技术栈简介

> 💡 "技术栈"就是搭建这个系统所用的工具集合，就像盖房子需要砖头、水泥、钢筋一样。

| 技术 | 用在哪里 | 通俗解释 |
|------|---------|---------|
| **Vue 3** | 前端 | 一个帮你高效构建网页界面的 JavaScript 框架。你看到的每个按钮、表格、弹窗都是用它"搭积木"搭出来的 |
| **TypeScript** | 前端 | JavaScript 的"加强版"，给代码加了类型检查（就像给每个变量贴了标签，减少拼写错误导致的 bug） |
| **Vite** | 前端 | 前端构建工具，负责把你写的代码"编译打包"成浏览器能运行的文件。类似一个超快的"翻译机" |
| **Element Plus** | 前端 | 现成的 UI 组件库（按钮、表格、弹窗等），不用从零画界面，直接"拿来用"。本项目用军绿色主题覆盖 |
| **Pinia** | 前端 | 状态管理工具——当多个页面需要共享同一份数据时（比如登录用户信息），它负责统一保管和分发 |
| **FastAPI** | 后端 | Python 写的 Web 框架，负责接收前端请求、处理业务逻辑、返回数据。自带 API 文档（访问 `/docs` 可看） |
| **SQLAlchemy** | 后端 | 数据库操作工具——让你用 Python 代码操作数据库，不用手写 SQL 语句 |
| **SQLite** | 数据库 | 一个"文件型"数据库——整个数据库就是一个 `.db` 文件，不需要安装数据库服务器，适合桌面应用 |
| **Electron** | 桌面壳 | 把网页变成桌面程序的技术（VS Code、微信桌面版都用了它）。让系统可以双击 .exe 运行 |
| **Alembic** | 后端 | 数据库"版本管理"——当表结构需要修改时（加字段、改类型），用它生成迁移脚本，安全升级 |
| **Pytest / Vitest** | 测试 | 自动化测试框架——写完代码后跑一遍测试，确认没有把已有功能搞坏 |
| **Docker** | 部署 | 把整个系统"装箱"——不管在什么电脑上，打开箱子就能跑，不用担心环境差异 |

---

## 常见文件类型说明

> 📁 看到文件后缀名不知道是什么？查这里：

| 后缀 | 全称 | 通俗解释 | 在哪里见到 |
|------|------|---------|-----------|
| `.vue` | Vue 单文件组件 | 一个页面或界面组件的完整代码（HTML + JS + CSS 三合一）。前端每个"页面"基本就是一个 `.vue` 文件 | `frontend/src/views/`、`frontend/src/components/` |
| `.ts` | TypeScript | 带类型的 JavaScript 代码文件。前端的逻辑、配置、工具函数都是 `.ts` | `frontend/src/` 各处 |
| `.py` | Python | 后端代码文件。所有服务器端逻辑（API、数据库操作、业务处理）都是 `.py` | `backend/` 各处 |
| `.json` | JSON 数据文件 | 一种通用的数据格式，像"结构化的记事本"。用于配置文件、依赖清单、数据交换 | 到处都有（`package.json`、`config.json` 等） |
| `.yml` / `.yaml` | YAML 配置文件 | 另一种配置格式，用缩进表示层级（比 JSON 更易读）。CI/CD 流程、Docker 编排用它 | `.github/workflows/`、`docker-compose.yml` |
| `.scss` | Sass 样式文件 | CSS 的"增强版"，支持变量和嵌套。前端界面颜色、间距、字体等样式定义在这里 | `frontend/src/styles/` |
| `.md` | Markdown 文档 | 纯文本格式的文档（就是你现在看的这种格式） | 项目根目录、`docs/` |
| `.sh` / `.bat` / `.ps1` | 脚本文件 | 自动化命令脚本。`.sh` 用于 Linux，`.bat`/`.ps1` 用于 Windows。双击或命令行运行 | `scripts/`、项目根目录 |
| `.spec` | PyInstaller 配置 | 告诉打包工具"怎么把 Python 代码变成 .exe"的配置 | `backend/` |
| `.ini` / `.toml` | 配置文件 | 各种工具的配置（pytest、mypy、alembic 等） | `backend/` |

---

## 如何找到我需要的文件？

> 🔍 **最常见的场景速查表**——"我想改 XX，该去哪个文件？"

| 🎯 我想… | 📂 去哪里找 | 💡 补充说明 |
|---------|-----------|-----------|
| 改系统版本号 | `version.txt` | 改完后运行 `python scripts/sync_version.py` 同步到各处 |
| 改某个页面的样子 | `frontend/src/views/` 对应目录 | 比如改帮扶村列表 → `views/analytics/supported-villages/` |
| 加一个新页面 | `frontend/src/views/` 新建 `.vue` 文件 | 然后去 `frontend/src/router/index.ts` 注册路由 |
| 改后端接口（API） | `backend/app/api/v1/` | 新建 `.py` 文件会被 `__init__.py` 自动注册 |
| 改数据库表结构 | `backend/app/models/` | 改完后用 Alembic 生成迁移：`cd backend && alembic revision --autogenerate -m "描述"` |
| 改菜单/权限 | `frontend/src/config/menu-config.ts` | 控制侧边栏显示哪些菜单项 |
| 改主题颜色 | `frontend/src/styles/tokens.scss` | 设计 Token 唯一数据源，所有颜色变量在这里定义 |
| 加 SCSS 变量 | `frontend/src/styles/tokens-vars.scss` | ⚠️ 只能放纯变量，禁止写 CSS 规则 |
| 改登录/注册逻辑 | 前端 `views/auth/` + 后端 `api/v1/auth/` | 前后端都要改 |
| 看/改系统配置 | `backend/app/core/config.py` | Settings 单例，包含所有后端配置项 |
| 初始化开发环境 | `scripts\dev-setup.bat`（Win）或 `bash scripts/dev-setup.sh`（Linux） | 一键安装所有依赖 |
| 构建 Windows 安装包 | 运行 `make win-installer` | 需要已安装 Node.js + Python |
| 构建麒麟 ARM64 包 | 运行 `make build-kylin` | 用于国产化部署 |
| 跑后端测试 | `cd backend && python -m pytest tests/ -v` | 1 万+ 测试用例 |
| 跑前端测试 | `cd frontend && npm test -- --run` | Vitest 单元测试 |
| 跑 E2E 测试 | `cd frontend && npx playwright test` | 模拟真实用户操作 |
| 看 API 文档 | 启动后端后访问 `http://localhost:8000/docs` | FastAPI 自动生成的交互式文档 |
| 改启动画面 | `electron/splash.html` | Electron 启动时显示的加载页 |
| 改应用图标 | `resources/bz.ico` + `resources/icons/` | 各尺寸图标 |

---

## 项目根目录总览

> 📦 打开项目文件夹，你会看到这些目录和文件。按"功能区域"分组说明：

### 🖥️ 核心代码（日常开发主要改这里）

| 路径 | 是什么 | 通俗说明 |
|------|--------|---------|
| `backend/` | Python 后端代码 | 所有服务器端逻辑：API 接口、数据库操作、业务处理、安全验证 |
| `frontend/` | Vue 前端代码 | 所有用户界面：页面、组件、样式、路由、状态管理 |
| `electron/` | Electron 桌面壳 | 把网页变成可双击运行的桌面程序（main.js + preload.js + splash.html + worker-pool） |

### 📚 文档与资源

| 路径 | 是什么 | 通俗说明 |
|------|--------|---------|
| `docs/` | 项目文档 | 4 大类 30+ 篇：快速开始 / 用户手册 / 开发文档 / 部署文档 |
| `resources/` | 静态资源 | 应用图标（bz.ico / icons/）、前端构建产物、VC++ 运行库、默认配置 |
| `deliverables/` | 交付报告 | 健康检查、故障检测、项目治理、QA 验证、UI 美化等报告 |

### 🔧 构建与部署

| 路径 | 是什么 | 通俗说明 |
|------|--------|---------|
| `scripts/` | 运维脚本 | 启动/停止/同步/审计/清理/覆盖率统计等自动化脚本 |
| `build-scripts/` | 构建脚本 | electron-builder NSIS 钩子 + 构建配置 + 部署包/Nuitka 构建 |
| `deploy/` | 部署配置 | 麒麟桌面/systemd/DEBIAN/polkit 配置（国产化 Linux 部署用） |
| `docker/` | Docker 文件 | 多架构容器构建（含麒麟 V10 单机版 + docker-compose.e2e.yml） |
| `k8s/` | Kubernetes 清单 | 集群部署配置（backend / frontend / configmap / pv） |
| `nginx/` | Nginx 配置 | 反向代理配置（nginx.conf + conf.d/），生产环境用 |
| `installers/` | NSIS 安装脚本 | Windows 安装程序脚本（x64 / x86 及 *_only 变体） |
| `dist/` | 构建产物输出 | Electron / Windows 安装包 / 后端 exe 的最终输出位置 |
| `build/` | 打包中间产物 | 分架构 PyInstaller / 安装包的临时构建文件 |

### 🗃️ 运行时数据

| 路径 | 是什么 | 通俗说明 |
|------|--------|---------|
| `data/` | 运行期数据 | SQLite 数据库文件、缓存、离线地图瓦片、令牌黑名单 |
| `data_sync/` | 同步数据 | 多台电脑之间同步用的数据 |
| `uploads/` | 上传文件 | 用户上传的附件存在这里 |
| `logs/` | 运行日志 | 系统运行产生的日志文件（出问题时先看这里） |

### 🧪 测试与质量

| 路径 | 是什么 | 通俗说明 |
|------|--------|---------|
| `tests/` | 系统级测试 | e2e / manual / performance / security / stability / ui-ux / tools |
| `.github/` | GitHub Actions | CI/CD 自动化（pr-checks + build-windows + build-arm64 + nightly-full） |

### 📄 根目录重要文件

| 文件 | 是什么 | 通俗说明 |
|------|--------|---------|
| `README.md` | 项目主文档 | 项目介绍、快速开始指南 |
| `LICENSE` | 开源协议 | MIT 协议——允许自由使用和修改 |
| `CONTRIBUTING.md` | 贡献指南 | 如何参与开发、提交代码的规范 |
| `CHANGELOG.md` | 变更日志 | 每个版本改了什么 |
| `CLAUDE.md` | AI 辅助开发说明 | 给 AI 编程助手看的项目上下文 |
| `AGENTS.md` | Agent 快速参考 | 构建命令 + 架构陷阱 + 安全清单 |
| `Makefile` | 统一任务入口 | 运行 `make test`、`make build`、`make win-installer` 等 |
| `package.json` | Electron 配置 | Electron 启动脚本 + electron-builder 构建编排 |
| `docker-compose.yml` | Docker 编排 | 一键启动多个 Docker 容器 |
| `version.txt` | 版本号唯一数据源 | 当前 1.8.1，经 `scripts/sync_version.py` 同步到各处 |
| `environment.yml` | Conda 依赖 | 重型科学计算依赖（numpy/pandas/scipy 等） |
| `build.sh` / `build.ps1` | 构建入口 | Linux 用 `.sh`，Windows 用 `.ps1` |
| `build-kylin.sh` | 麒麟构建入口 | 麒麟 ARM64 专用构建脚本 |
| `run.ps1` | Windows 运行脚本 | 一键启动系统 |
| `mypy.ini` | 类型检查配置 | Python 静态类型检查规则 |
| `.pre-commit-config.yaml` | Git 钩子配置 | 提交代码前自动检查（两阶段：pre-commit + pre-push） |
| `commitlint.config.js` | 提交信息规范 | 强制 Conventional Commit 格式 |
| `项目文件结构说明.md` | 本文件 | 你正在看的这个文件 😄 |

---

## backend/ — 后端（服务器端）

> 👨‍🍳 后厨的全部家当。用 Python + FastAPI 编写，负责处理所有数据逻辑。

### 后端整体结构（一图看懂）

```
backend/
├── app/                    ← 📌 核心代码都在这里
│   ├── main.py            ← 应用入口（程序从这里启动）
│   ├── config.py          ← 应用配置
│   ├── api/v1/            ← 🌐 API 接口（前端调用的"菜单"）
│   ├── core/              ← 🧱 基础设施（安全、数据库、缓存等"水电煤"）
│   ├── models/            ← 🗄️ 数据模型（数据库表的 Python 描述）
│   ├── schemas/           ← 📐 数据校验模板（请求/响应的格式定义）
│   ├── services/          ← ⚙️ 业务逻辑（真正"干活"的代码）
│   ├── middleware/        ← 🚧 中间件（每个请求都要经过的"安检门"）
│   ├── utils/             ← 🔨 工具函数（通用小工具）
│   └── interfaces/        ← 📋 接口抽象层
├── tests/                  ← 🧪 测试代码（1 万+ 用例）
├── scripts/                ← 🔧 运维/诊断脚本（38 个）
├── alembic/                ← 📊 数据库迁移记录
├── start.py                ← 🚀 启动入口（兼容打包后的 exe）
├── requirements.txt        ← 📋 Python 依赖清单
└── pytest.ini              ← 测试配置
```

### backend/ 根目录文件

| 文件 | 通俗说明 |
|------|---------|
| `start.py` | **程序启动入口**：处理了 Windows 兼容性（事件循环修复、GBK 编码修复），打包成 exe 后也从这里启动 |
| `run_tests.py` | 一键跑测试的脚本 |
| `pytest.ini` | 测试框架配置（异步测试 + 并行执行） |
| `pyproject.toml` | Python 项目的"身份证"（名称、版本、构建方式） |
| `requirements.txt` | 依赖清单——"这个系统需要安装哪些 Python 库" |
| `requirements-*.txt` | 不同场景的依赖变体（docker / dev / prod / build / minimal / optional / lock） |
| `alembic.ini` | 数据库迁移工具的配置 |
| `Dockerfile` | 告诉 Docker "怎么把后端装进容器" |
| `mypy.ini` | 类型检查配置 |
| `assistance-backend.spec` | PyInstaller 打包配置（把 Python 变成 Windows .exe） |
| `backend_linux_arm64.spec` / `backend_linux_arm64_standalone.spec` | 麒麟 ARM64 打包配置 |
| `version.json` | 后端版本信息 |

### backend/app/api/v1/ — API 接口层（47 个路由 + 5 个子包）

> 🌐 这是前端和后端沟通的"窗口"。每个文件对应一组功能接口。前端发请求到某个 URL，后端的对应文件处理它。

**核心业务接口（最常改动的）：**

| 文件 | 管什么 | 通俗说明 |
|------|--------|---------|
| `organization.py` | 组织机构管理 ⭐ | 组织树增删改查 + 统计概览 + 成员管理 + 导出 + 拖拽排序 + 停用/激活 + 移动组织 |
| `supported_village.py` | 帮扶村管理 ⭐ | 系统的核心功能：村庄信息的增删改查 + 10 板块年度数据 + 导入导出 + 附件 |
| `supported_village_export.py` | 帮扶村导出 | 专门处理数据导出（注册顺序在主模块之前，避免路由冲突） |
| `funds.py` | 经费管理 ⭐ | 帮扶经费的增删改查 + 状态流转（申请→审批→拨付→使用） |
| `fund_budgets.py` | 经费预算 | 预算编制和管理 |
| `fund_lifecycle.py` | 经费生命周期 | 经费从申请到结项的全过程跟踪 |
| `projects.py` | 项目管理 ⭐ | 帮扶项目的增删改查 + 导入导出 + 里程碑 |
| `project_milestones.py` | 项目里程碑 | 项目关键节点管理 |
| `school.py` | 帮扶学校 | 学校信息 + 受助学生导入 |
| `policy.py` | 帮扶政策 | 政策发布/搜索/收藏/导入导出 |

**系统管理接口：**

| 文件 | 管什么 |
|------|--------|
| `organization.py` | 组织机构管理（单位树） |
| `approval.py` | 审批流程 |
| `assessment.py` | 帮扶成效评估 |
| `menus.py` | 动态菜单权限 |
| `messages.py` / `messages_extended.py` | 消息通知 |
| `work_logs.py` | 工作日志 |
| `rural_works.py` | 乡村振兴工作记录 |
| `rural_tasks.py` | 乡村任务管理 |
| `villages.py` / `village_templates.py` | 自然村管理 / 村庄模板 |
| `user_permissions.py` / `permission_package.py` | 用户权限 / 权限包 |

**数据与同步接口：**

| 文件 | 管什么 |
|------|--------|
| `map.py` / `offline_map.py` | 地图数据 / 离线瓦片地图 |
| `data_sync.py` / `sync.py` | 数据同步 / 多机协同 |
| `data_quality.py` / `data_scope.py` | 数据质量检查 / 数据权限范围 |
| `encryption.py` / `machine_code.py` | 数据加密 / 设备绑定 |
| `batch_operations.py` | 批量操作 |

**分析与辅助接口：**

| 文件 | 管什么 |
|------|--------|
| `effectiveness.py` | 帮扶成效分析 |
| `sentiment.py` | 舆情监控 |
| `ai.py` / `ai_enhanced.py` | AI 辅助功能 |
| `search.py` | 全文搜索 |
| `system_health.py` / `system_config.py` / `performance.py` | 系统健康 / 配置 / 性能监控 |
| `feedback.py` / `todos.py` | 用户反馈 / 待办事项 |
| `validation.py` / `report_templates.py` | 数据校验 / 报表模板 |
| `deps.py` | 共享依赖注入（多个接口共用的"前置检查"） |
| `reminders.py` | 提醒中心（审批超时/项目截止/预算预警聚合 + 手动扫描） |
| `effectiveness.py` | 帮扶成效评估 + 年度考核闭环（评估后生成复核审批任务） |
| `monitoring_legacy.py` | 旧版监控接口（兼容保留） |
| `__init__.py` | 自动扫描注册所有路由模块（新增文件不用手动注册） |

**子包路由（更复杂的功能模块）：**

| 目录 | 包含什么 | 通俗说明 |
|------|---------|---------|
| `auth/` | 登录、注册、Token、密码重置、2FA、RBAC、用户管理 | 所有跟"用户身份"相关的功能 |
| `system/` | 19 个模块：admin、audit、backup、cache、config、env、health、help（27篇帮助文档）、i18n、init、metrics、monitor 等 | 系统运维管理功能；backup 含 U 盘/移动硬盘备份目标检测（`/backup/dirs`）、一键备份（可加密）、**上传备份包恢复**（`/backup/upload-restore`，支持任意机器导出的加密/明文备份包） |
| `data/` | 数据分析、统计、仪表盘、数据包、数据质量、报表 | 所有跟"看数据、分析数据"相关的功能 |
| `import_export/` | 异步导出、分块上传、导入 | 大文件导入导出的专门处理 |
| `monitoring/` | 指标采集、密钥轮换、数据分级 | 系统监控和安全 |

### backend/app/core/ — 核心基础设施（47 个模块）

> 🧱 这是系统的"水电煤"——安全、数据库连接、缓存、日志等底层能力。一般不需要频繁修改。

| 分类 | 关键文件 | 通俗说明 |
|------|---------|---------|
| **配置** | `config.py` | 系统总配置（Settings 单例，PROJECT_VERSION=1.8.1） |
| **安全认证** | `security.py` | JWT 令牌签发/验证、密码加密（bcrypt）、Token 黑名单 |
| | `token_manager.py` / `token_blacklist.py` | 登录令牌管理（让"退出登录"真正生效） |
| | `cookie_security.py` | Cookie 安全设置 |
| **数据库** | `database.py` | 数据库连接引擎 + 会话工厂（所有数据库操作的起点） |
| | `database_compat.py` / `database_indexes.py` / `database_root.py` | 兼容层 / 索引管理 / 根配置 |
| **数据权限** | `data_permission.py` | 按组织/角色过滤数据（"你只能看你权限范围内的数据"） |
| | `data_scope_adapter.py` | 统一数据权限入口（整合了三种历史实现） |
| | `unified_data_scope.py` | 组织树过滤（OrgScopeFilter） |
| **缓存** | `cache.py` / `cache_settings.py` | 双层缓存（diskcache 磁盘 + 内存 LRU），加速频繁查询 |
| **审计日志** | `audit.py` / `audit_middleware.py` | 记录"谁在什么时间做了什么操作" |
| **错误处理** | `errors.py` / `error_handler.py` / `exceptions.py` | 统一错误格式（前端能看懂的错误信息） |
| **日志** | `logging.py` / `logging_config.py` / `structured_logging.py` | 系统日志配置 |
| **文件上传** | `file_upload.py` / `file_utils.py` / `upload_security.py` | 文件上传处理 + 安全校验 |
| **其他** | `events.py`（事件总线）、`i18n.py`（国际化）、`performance.py`（性能优化）、`transaction.py`（事务管理）、`query_optimizer.py`（SQL 优化）、`response.py`（统一响应格式）、`build_info.py`（构建指纹）、`prophet_status.py`（预测库检测）等 | 各种底层支撑能力 |

### backend/app/models/ — 数据模型（63 个文件 / 120+ 个模型类）

> 🗄️ 每个模型类 = 数据库里的一张表。改表结构就改这里。

**核心业务模型：**

| 文件 | 对应什么表 | 通俗说明 |
|------|-----------|---------|
| `supported_village.py` | 帮扶村 | 19 个基本字段 + 10 个年度子表 + 帮扶经费 + 村委信息（系统最核心的表） |
| `fund.py` | 经费 | 经费全生命周期字段 + 附件 + 预算记录 |
| `fund_budget.py` / `fund_lifecycle.py` / `fund_history.py` / `fund_allocation_order.py` / `fund_asset_verification.py` | 经费相关 | 预算、生命周期、变更历史、拨付单、资产核验 |
| `project.py` / `project_milestone.py` | 帮扶项目 | 项目信息 + 任务 + 文件 + 里程碑 + 变更日志 |
| `school.py` | 帮扶学校 | 学校 + 帮扶关系 + 项目 + 受助学生 + 附件 |
| `policy.py` | 帮扶政策 | 政策 + 分类 + 收藏 |
| `user.py` / `user_session.py` | 用户 | 用户账号 + 角色关联 + 会话 |
| `organization.py` | 组织机构 | 单位树结构 |
| `rbac.py` / `role.py` | 权限 | 角色、权限、资源访问控制 |
| `approval.py` | 审批 | 审批流程 + 节点 + 任务 + 记录 |

**年度数据子表（帮扶村的 10 个板块）：**

| 文件 | 记录什么 |
|------|---------|
| `annual_income.py` | 年度收入数据 |
| `annual_industry.py` | 年度产业数据 |
| `annual_infrastructure.py` | 年度基础设施数据 |
| `annual_population.py` | 年度人口数据 |

**其他模型：** `message.py`（消息）、`audit.py`（审计日志）、`todo.py`（待办）、`sentiment.py`（舆情）、`data_sync.py`（同步日志）、`system_config.py`（系统配置）、`monitoring.py`（监控指标）、`work_log.py`（工作日志）、`village.py`（自然村）、`army_unit.py`（部队单位）、`machine_code.py`（设备码）等。

**特别说明：** `__init__.py` 使用**懒加载**机制（`__getattr__` 延迟导入），120+ 个模型类通过 `_MODULE_MAP` 映射，用到哪个才加载哪个，加快启动速度。`base.py` 定义了所有模型的公共基类（BaseModel、时间戳、软删除、版本号）。

### backend/app/schemas/ — 数据校验模板（25 个模块）

> 📐 Schema = "数据长什么样"的定义。前端发来的数据必须符合 Schema 定义的格式，否则会被拒绝。

| 分类 | 文件 | 管什么 |
|------|------|--------|
| 用户与权限 | `user.py`、`auth.py`、`role.py`、`permission.py`、`permission_package.py` | 登录/注册/角色/权限的数据格式 |
| 业务实体 | `supported_village.py`、`village.py`、`project.py`、`fund.py`、`school.py`、`policy.py`、`rural_work.py`、`rural_task.py`、`army_unit.py`、`organization.py` | 各业务模块的数据格式 |
| 数据与报表 | `data_package.py`、`data_package_encrypted.py`、`data_report.py`、`document.py`、`template_fields.py` | 数据包和报表的格式 |
| 系统与通用 | `common.py`、`audit_log.py`、`activity_log.py`、`monitor.py`、`resource.py` | 通用响应、审计、监控格式 |

### backend/app/services/ — 业务逻辑层（100 个服务 + 8 个子包）

> ⚙️ 这是真正"干活"的代码。API 接口收到请求后，调用 Service 来处理具体业务。

**按功能分组：**

| 功能域 | 主要服务文件 | 做什么 |
|--------|------------|--------|
| 🏘️ 帮扶村 | `supported_village_service.py`、`supported_village_export_service.py`、`village_cascade_delete_service.py` | 村庄数据增删改查、导出、级联删除 |
| 💰 经费 | `fund_service.py`、`fund_anomaly_detector.py`、`fund_anomaly_engine.py`、`fund_event_handler.py`、`fund_health_service.py`、`fund_project_linker.py` | 经费管理 + 异常检测 + 健康度分析 |
| 📋 项目 | `project_service.py` | 项目增删改查 |
| 📜 政策 | `policy_service.py`、`policy_import_service.py`、`policy_fts_service.py` | 政策管理 + Excel 导入 + 全文搜索 |
| 🔐 权限 | `rbac_service.py`、`user_permission_service.py`、`organization_permission_service.py`、`machine_code_permission_service.py`、`permission_package_service.py` | 角色权限管理 |
| 👤 用户 | `user_service.py`、`user_cascade_delete_service.py`、`lockout_service.py`、`two_factor_service.py` | 用户管理 + 锁定 + 两步验证（token 黑名单统一走 `core/token_blacklist.py`，异步版服务已删除） |
| 📬 消息 | `message_service.py`、`message_template_service.py`、`notification_preference_service.py` | 消息通知 |
| 🏢 组织 | `organization_service.py`、`organization_code_service.py` | 组织机构管理 |
| ⚙️ 系统 | `system_config_service.py`、`config_package_service.py`、`update_log_service.py`、`version_service.py` | 系统配置 |
| 📝 审计 | `audit_service.py`、`audit_enhancement_service.py`、`audit_event_handler.py`、`log_export_service.py` | 操作审计 |
| 🔄 同步 | `data_sync_service.py`、`data_sync_encryption_service.py`、`data_sync_enhanced.py` | 多机数据同步 |
| ✅ 数据质量 | `data_quality_scorer.py`、`data_validator_service.py`、`data_cleaning_service.py`、`data_masking_service.py` | 数据校验/清洗/脱敏 |
| 📦 数据包 | `data_package_service.py`、`package_version_service.py`、`encrypted_package.py`、`import_export_history_service.py`、`data_tier_service.py` | 数据包版本管理 |
| 🔒 加密 | `encryption_service.py`、`password_encryption_service.py`、`secrets_manager.py`、`sm_crypto.py`、`aes_gcm_cipher.py` | 数据加密（含国密 SM 算法） |
| 📊 报表 | `report_service.py`、`report_export_service.py`、`docx_service.py`、`pdf_service.py`、`excel_template_service.py` | 导出 Excel/PDF/Word |
| 📥 导入导出 | `export_service.py`、`excel_importer_service.py`、`batch_service.py`、`async_export_service.py`、`chunked_upload_service.py` 等 | 大文件分批处理 |
| 📈 监控 | `health_service.py`、`metrics_service.py`、`monitoring_service.py`、`alert_service.py`、`resource_monitor.py` 等 | 系统健康监控 |
| 🤖 AI | `ai_service.py`、`smart_conflict_resolver.py`、`smart_report.py` | AI 辅助分析 |
| 🌾 乡村工作 | `rural_work_service.py` | 工作记录 CRUD + 统计 + 报告 + 批量删除 |
| 🛡️ 单机安全 | `immediate_backup.py` | 关键操作（导入/批量删除）后即时备份（后台线程+幂等锁） |
| 🔔 提醒 | `reminder_orchestrator.py` | 统一提醒扫描接线：审批超时/项目截止/预算预警 → 写入消息表（幂等） |
| 📦 数据包 | `data_package_service.py` | 支持增量导出（`incremental` + `since_sync_version` 按 sync_version 过滤） |

**子包（更细化的功能模块）：**

| 子包 | 内容 |
|------|------|
| `ai/` | 异常检测、NLP 查询、推荐、趋势预测 |
| `funding/` | 阶段预算、阶段初始化 |
| `repositories/` | 数据访问层（base、fund_repository） |
| `sentiment/` | 舆情分析、爬虫 |
| `village/` | 数据权限、mixins |
| `zero_trust/` | 设备指纹、动态权限、中间件 |
| `approval/`、`project/` | 占位包（预留扩展） |

### backend/app/middleware/ — 中间件层（14 个）

> 🚧 中间件 = 每个请求都要经过的"检查站"。在请求到达 API 之前（或响应返回之前）做统一处理。

| 文件 | 做什么 | 通俗说明 |
|------|--------|---------|
| `csrf_middleware.py` | CSRF 防护 | 防止恶意网站冒充用户发请求 |
| `camel_to_snake.py` | 字段名转换 + 响应补全 | 前端 camelCase 请求体→后端 snake_case；响应裸 dict 自动补全 `code/success/message` 信封元数据 |
| `audit_middleware.py` | 操作审计 | 自动记录每个操作 |
| `metrics_middleware.py` | 指标收集 | 统计每个接口的调用次数、耗时 |
| `request_id.py` | 请求 ID | 给每个请求一个唯一编号（方便追踪问题） |
| `request_logger.py` | 请求日志 | 记录所有请求 |
| `body_size_limit.py` | 大小限制 | 防止上传超大文件把服务器撑爆 |
| `cache_middleware.py` / `cache_headers.py` | HTTP 缓存 | 减少重复请求 |
| `api_version.py` | 版本管理 | API 版本控制 |
| `query_counter.py` | SQL 计数 | 监控一次请求执行了多少条 SQL（防性能问题） |
| `slow_request_monitor.py` | 慢请求检测 | 找出响应太慢的接口 |
| `audit_context.py` / `utils.py` | 审计上下文 / 工具 | 辅助功能 |

### backend/app/utils/ — 工具层（31 个模块）

> 🔨 通用小工具，被各个模块复用。

| 分类 | 文件 | 做什么 |
|------|------|--------|
| 分页 | `pagination.py`、`cursor_pagination.py` | 数据太多时"翻页"显示 |
| 数据库 | `database_init.py`、`database_migration.py`、`db_error_handler.py`、`db_metrics.py`、`db_performance.py`、`query_optimizer.py` | 数据库初始化/迁移/错误处理/性能 |
| 安全 | `encryption.py`、`security_enhanced.py`、`input_validator.py`、`runtime_secrets.py`、`secret_migration.py`、`permission_filter.py`、`audit_logger.py` | 加密/校验/权限过滤 |
| 系统 | `performance.py`、`system_metrics.py`、`scheduler_tasks.py`、`win_proactor_fix.py`、`async_executor.py`、`retry.py` | 性能监控/定时任务/Windows 兼容/重试 |
| 其他 | `api_error.py`、`chart.py`、`common.py`、`date_type_handler.py`、`email.py`、`file_response.py`、`helpers.py`、`init_permissions.py`、`paths.py`、`upload_helper.py` | 各种辅助功能 |
| 磁盘检测 | `drive_detect.py` | 检测可移动磁盘/U 盘/网络盘（备份目标选择用，跨平台） |

### backend/app/interfaces/ — 接口抽象层

| 目录 | 内容 |
|------|------|
| `api/v1/` | 版本化 API 接口抽象 |
| `schemas/responses.py` | 统一响应模型 |

### backend/tests/ — 测试（12,163 个用例）

> 🧪 测试 = 自动化的"质量检查员"。每次改完代码跑一遍，确认没有搞坏已有功能。

| 目录/文件 | 内容 | 通俗说明 |
|-----------|------|---------|
| `unit/` | 330+ 个单元测试文件 | 测试每个小函数是否正确（含 `api/`、`services/`、`middleware/`、`utils/` 子目录；大量 `*_cov.py` 覆盖率攻坚测试） |
| `integration/` | 集成测试 | 测试多个模块配合是否正常（认证、审计、加密包、搜索等） |
| `security/` | 安全测试 | 测试安全机制是否有效（审计、数据隔离、重试） |
| `factories/` | 测试工厂 | 快速生成测试数据（user / village / fund / project / school / organization / approval / common） |
| `helpers/` | 断言辅助 | 测试用的通用验证函数 |
| `conftest.py` / `conftest_extended.py` | 共享 fixtures | 所有测试共用的初始化配置 |
| 顶层 `test_*.py`（16 个） | 冒烟/综合测试 | 快速验证系统基本功能 |
| `COVERAGE_100_ROADMAP.md` | 覆盖率路线图 | 测试覆盖率提升计划 |

### backend/scripts/ — 运维/诊断脚本（38 个）

> 🔧 系统维护和故障排查用的工具箱。

| 分类 | 代表脚本 | 什么时候用 |
|------|---------|-----------|
| 诊断分析 | `diagnose_database.py`、`diagnose_auth_issues.py`、`deep_scan.py`、`profile_api.py`、`performance_benchmark.py` | 系统出问题时的"体检工具" |
| 扫描校验 | `verify_all_imports.py`、`validate_system.py`、`security_self_check.py`、`system_health_check.py` | 定期"安检" |
| 数据检查 | `check_admin.py`、`check_foreign_keys.py`、`check_schools.py` | 检查数据完整性 |
| 修复 | `fix_admin_permissions.py`、`fix_database.py`、`fix_sort_order.py` | 数据出问题时的"修复工具" |
| 运维 | `start_system.py`、`cleanup_logs.py`、`reset_admin_password.py`、`optimize_database.py`、`manage_keys.py` | 日常运维操作 |

### backend/alembic/ — 数据库迁移

> 📊 记录数据库表结构的每次变更。就像数据库的"版本历史"。

| 文件 | 说明 |
|------|------|
| `versions/012_consolidate_baseline.py` | v1.2.0 基线合并：所有增量迁移合为一个起点，新部署从这里开始 |
| `versions/013_bootstrap_baseline.py` | 引导基线修复（2026-07-18）：解决空数据库无法初始化的问题 |

---

## frontend/ — 前端（用户界面）

> 🍽️ 前厅的全部装修。用 Vue 3 + TypeScript + Vite 编写，用户看到的一切都在这里。

### 前端整体结构（一图看懂）

```
frontend/src/
├── views/          ← 📄 页面（每个功能模块一个目录）
├── components/     ← 🧩 通用组件（多个页面复用的 UI 零件）
├── stores/         ← 🗃️ 状态管理（Pinia，跨页面共享数据）
├── router/         ← 🗺️ 路由配置（URL 和页面的对应关系）
├── api/            ← 📡 API 请求封装（调用后端的代码）
├── types/          ← 📐 TypeScript 类型定义
├── styles/         ← 🎨 样式文件（颜色、间距、主题）
├── composables/    ← 🔧 组合式函数（可复用的逻辑片段）
├── config/         ← ⚙️ 配置（菜单、枚举、常量）
├── directives/     ← 📌 自定义指令（权限控制、水印）
├── utils/          ← 🔨 工具函数
├── layouts/        ← 📐 页面布局框架
├── assets/         ← 🖼️ 静态资源（图片等）
└── data/           ← 📊 静态数据（贵州行政区划等）
```

### frontend/src/views/ — 页面视图（34 个目录 + 1 个根文件）

> 📄 每个目录 = 一个功能模块的所有页面。这是你改界面时最常来的地方。

**根文件：**

| 文件 | 是什么 |
|------|--------|
| `HomeSafe.vue` | 军事风工作台首页（登录后看到的第一个页面） |
| `NotFound.vue` | 404 页面（访问不存在的地址时显示） |

**核心业务页面：**

| 目录 | 对应功能 | 包含哪些页面 |
|------|---------|-------------|
| `analytics/supported-villages/` | 🏘️ 帮扶村管理 | 列表（多条件筛选）、详情（编辑）、表单、年度数据录入、年度总览、板块数据表单 |
| `funds/` | 💰 经费管理 | 列表、创建/编辑、审批、分析、预算、生命周期、经费申请 |
| `projects/` | 📋 项目管理 | 项目管理主页、列表、编辑、导入 |
| `schools/` | 🏫 帮扶学校 | 列表、详情、分析 |
| `policies/` | 📜 帮扶政策 | 列表、详情、收藏 |
| `ruralWorks/` | 🌾 乡村工作 | 工作记录管理 |

**数据与分析页面：**

| 目录 | 对应功能 |
|------|---------|
| `dashboard/` | 工作台（KPI 卡片、图表、快捷操作、全局搜索、自定义布局编辑器） |
| `bigscreen/` | 帮扶成效大屏（汇报模式，全屏自动轮播 KPI/经费趋势/成效排名/项目状态） |
| `analytics/dashboard/` | 数据分析面板 |
| `analytics/map/` | 离线地图可视化 |
| `analytics/reports/` / `analytics/Assessment.vue` | 分析报表 / 成效评估 |
| `dataAnalysis/` | 数据分析 |
| `dataPackage/` | 数据包版本管理 |
| `dataEntry/` | 综合数据录入 |
| `dataSync/` | 多机协同同步 |
| `dataVerify/` | 数据校验 |
| `dataManagement/` | 数据管理（导出） |
| `dataImport/` / `import/` | 数据导入 |
| `export/` | 数据导出 |

**系统管理页面：**

| 目录 | 对应功能 |
|------|---------|
| `system/` | 监控面板、用户管理、系统初始化、备份管理、缓存监控 |
| `auth/` | 登录、注册、个人中心、重置密码 |
| `auth/LoginEnhanced.vue` | 登录页（含"记住登录"勾选：持久化令牌实现本机自动登录，默认关闭） |
| `organization/` | 组织机构管理 |
| `approval/` | 审批管理 |
| `admin/` | 管理员面板 |

**其他功能页面：**

| 目录 | 对应功能 |
|------|---------|
| `message/` | 消息中心 |
| `todos/` | 待办事项 |
| `report/` / `reportTemplates/` | 报表管理 / 报表模板 |
| `sentiment/` | 舆情监控 |
| `effectiveness/` | 帮扶成效 |
| `batch/` | 批量操作 |
| `ai/` | AI 辅助 |
| `help/` | 帮助文档 |
| `work-calendar/` | 工作日历 |
| `reminder/` | 提醒中心（审批超时/项目到期/预算预警聚合展示 + 手动扫描） |
| `ruralWorks/checkin/` | 驻村工作台（今日打卡 + 月度总结聚合展示） |
| `errorPage/` | 错误页面（403/404/500） |

### frontend/src/ 其他重要目录

| 目录 | 是什么 | 通俗说明 |
|------|--------|---------|
| `components/` | 通用组件 | 多个页面复用的 UI 零件（图表 EnhancedChart、数据表 DataTable、表单向导 FormWizard、业务卡片 VillageCard/ProjectCard） |
| `stores/` | Pinia 状态仓库（19 个） | 跨页面共享的数据中心：auth（登录状态）、menu（菜单）、village（村庄数据）、funds（经费）等 |
| `router/` | 路由配置 | URL 和页面的对应关系 + 路由守卫（未登录自动跳转登录页） |
| `api/` | API 请求封装 | 48 个模块的接口调用代码 + 请求拦截器（自动加 Token、统一错误处理）；含 reminders.ts（提醒中心） |
| `types/` | 类型定义（10 个） | TypeScript 类型：entities / models / api / rbac / organization / policy 等 |
| `config/` | 配置 | 菜单配置（menu-config.ts）、应用配置、枚举、区域字典、常量 |
| `constants/` | 常量 | 数据类型常量（dataTypes.ts） |
| `data/` | 静态数据 | 贵州行政区划数据（guizhouRegion.ts） |
| `styles/` | 样式体系 | 见下方详细说明 |
| `composables/` | 组合式函数（31 个） | 可复用的逻辑片段：useRouterSafe（安全跳转）、usePrint（打印）、useECharts（图表）、useLoading（加载状态）、useAutoLock（自动锁屏）等（v1.8.0 删除无引用的 useMessageNotification） |
| `directives/` | 自定义指令 | permission（按钮级权限控制）、watermark（页面水印） |
| `utils/` | 工具函数（26 个） | enhancedStorage、LocalDatabase、treeNormalizer（树节点规范化）、crypto、jwt、logger、camelize（snake↔camel 双向转换）、chartColors（ECharts 场景运行时读取 CSS 变量色值，v1.8.0 新增）等 |
| `layouts/` | 布局组件 | DefaultLayoutSafe.vue（含 <768px 侧边栏自动收起 + 移动端底部导航） |
| `micro-fe/` | 微前端配置 | config.ts |
| `assets/` | 静态资源 | 图片等 |
| `test/` | 测试基建 | setup.ts、factories.ts |

### 样式体系说明（styles/ 目录）

> 🎨 改界面外观时看这里。

| 文件 | 作用 | ⚠️ 注意事项 |
|------|------|-----------|
| `tokens.scss` | **设计 Token 唯一数据源**：所有颜色、间距、字体变量在这里定义（CSS 变量 + SCSS 映射） | 改主题色只改这里 |
| `tokens-vars.scss` | 纯 SCSS 变量（被 Vite 自动注入到每个组件） | ⚠️ **禁止写任何实体 CSS 规则**（不能写 :root、选择器） |
| `index.scss` | 全局样式入口 | `--el-*` 变量用 var() 引用 token |
| `components/*.scss` | 共享页面样式 | list-page / form-page / table / form / layout 等 |
| `dashboard-theme.scss` | 工作台主题 | |
| `responsive.scss` | 响应式适配 | |
| `print.scss` / `print.css` | 打印样式 | |
| `accessibility.css` | 无障碍样式 | |

### Frontend 技术栈版本

| 技术 | 版本/说明 |
|------|----------|
| Vue | 3.x |
| TypeScript | 5.x |
| Vite | 5.x |
| Pinia | 状态管理 |
| Vue Router | 4.x |
| Element Plus | 2.6.x（CSS 变量覆盖为军绿主题） |
| Sass | 1.101.0（modern-compiler API） |
| Vitest | 单元测试 |
| Playwright | E2E 测试 |
| ESLint + Prettier | 代码规范 |
| lint-staged + Husky | Git hooks |

### 前端测试

| 目录/文件 | 内容 | 通俗说明 |
|-----------|------|---------|
| `frontend/tests/unit/` | 347 个单元测试文件（全仓 351 个测试文件 / 6,387 个用例） | 测试组件、API 封装、stores、工具函数等 |
| `frontend/tests/property/` | 属性测试 | auth 持久化测试 |
| `frontend/tests/e2e/` | E2E 流程测试 | 模拟完整用户操作流程 |
| `frontend/e2e/` | Playwright 冒烟测试 | 5 条关键路径（smoke.spec.ts） |
| `frontend/vitest.config.ts` | 测试配置 | hookTimeout: 10000, testTimeout: 15000, 环境: jsdom |

---

## electron/ — 桌面应用外壳

> 📦 把网页"装进盒子"变成桌面程序。

| 文件 | 是什么 | 通俗说明 |
|------|--------|---------|
| `main.js` | Electron 主进程 | 桌面程序的"总管"：创建窗口、管理生命周期；含系统托盘（未读角标+快捷菜单）、全局快捷键（Ctrl+Alt+N/T/B/D）、开机自启、关闭行为确认 |
| `preload.js` | IPC 通信桥接 | 网页和桌面程序之间的"翻译官"（安全隔离）；暴露托盘角标、导航订阅、开机自启等 API |
| `splash.html` | 启动画面 | 程序启动时显示的加载动画 |
| `worker-pool.js` / `worker-tasks.js` | 后台工作线程池 | 耗时任务在后台线程执行，不卡界面 |
| `tests/` | Electron 测试 | 桌面壳的测试 |

---

## 其他重要目录

| 路径 | 通俗说明 |
|------|---------|
| `resources/bz.ico` / `resources/icons/` | 应用图标（各种尺寸） |
| `resources/frontend/` | 前端构建产物（生产部署时 Electron 加载这里） |
| `resources/config/default.json` | 默认配置文件 |
| `scripts/dev-setup.bat` / `scripts/dev-setup.sh` | 一键初始化开发环境（安装所有依赖） |
| `scripts/migrate-to-64bit.bat` / `scripts/migrate-to-64bit.sh` | 64-bit Python 迁移脚本 |
| `scripts/sync_version.py` | 版本号同步工具（改 version.txt 后运行） |
| `scripts/check_menu_alignment.py` | 前后端菜单对齐检查（CI 门禁：前端菜单 key/path 与后端 MENU_DEFINITIONS/路由双向校验） |
| `scripts/pre_commit_hooks.py` | 代码提交前的质量检查编排 |
| `build-scripts/electron-builder-nsis-hook.nsh` | Windows 安装程序自定义钩子 |
| `build-scripts/build-config.json` | 构建配置 |
| `build-scripts/build_deployment_packages.py` | 部署包构建 |
| `build-scripts/build_nuitka_arm64.py` | ARM64 构建 |
| `deploy/kylin/` | 麒麟 V10 部署全套配置（desktop / systemd / DEBIAN / scripts / config / polkit） |
| `docker/` | Docker 多架构构建文件（Dockerfile、Dockerfile.arm64、Dockerfile.kylin-standalone 等 + docker-compose.e2e.yml） |
| `k8s/` | Kubernetes 集群部署清单（backend.yaml、frontend.yaml、configmap.yaml、pv.yaml） |
| `nginx/` | Nginx 反向代理配置（生产环境用） |
| `Makefile` | 统一命令入口（`make test` / `make build` / `make win-installer` / `make build-kylin`） |
| `docs/` | 项目文档（01-快速开始 / 02-用户手册 / 03-开发文档 / 04-部署文档，共 30+ 篇） |
| `.github/ISSUE_TEMPLATE/` | Issue 模板（bug_report / feature_request） |

---

## CI/CD 自动化流程

> 🤖 CI/CD = 代码提交后自动运行的"流水线"。不用人手动跑测试、打包，机器自动完成。

### PR 门禁检查（pr-checks.yml）

> 每次提交代码（PR）都会自动跑这些检查，全部通过才能合并。

| 检查项 | 做什么 | 通俗说明 |
|--------|--------|---------|
| Backend Lint | ruff + flake8 | 检查 Python 代码风格 |
| Backend Test | pytest（1 万+ 用例） | 跑所有后端测试 |
| Frontend Lint | ESLint + Prettier | 检查前端代码风格 |
| Frontend Type Check | vue-tsc --noEmit | 检查 TypeScript 类型错误 |
| Frontend Unit Test | vitest run（351 个文件） | 跑所有前端测试 |
| Build Verification | 前端构建 | 确认代码能成功打包 |

触发条件：push 到任何分支 或 PR 到 main。

### 每日全量构建（nightly-full.yml）

> 每天凌晨 2 点（UTC）自动跑一遍完整检查。

| 阶段 | 内容 |
|------|------|
| Full Test Suite | 后端 1 万+ 用例 + 前端全量测试 |
| E2E Smoke Tests | Playwright 5 条关键路径 |
| Cross-platform Build | Windows x64/x86 + ARM64 .deb |
| Docker Build | 多架构镜像构建 |
| Security Audit | pip-audit + npm audit（检查依赖有无安全漏洞） |
| Artifact Upload | 构建产物归档 |

### 其他工作流

| 文件 | 做什么 |
|------|--------|
| `build-windows.yml` | Windows 安装程序构建（详见 docs/04-部署文档/01-Windows部署/构建打包指南.md） |
| `build-arm64.yml` | 麒麟 V10 ARM64 .deb 安装包构建 |

---

## Pre-commit 工作流

> 🚦 代码提交前的"自动安检"。配置在 `.pre-commit-config.yaml`。

### 两阶段策略

| 阶段 | 什么时候触发 | 检查什么 |
|------|------------|---------|
| **pre-commit** | `git commit` 时 | 尾部空格、文件结尾换行、YAML/JSON 格式、大文件（≤1MB）、合并冲突标记、ruff 代码规范（仅 backend/）、Dockerfile 格式 |
| **pre-push** | `git push` 时 | flake8（全量）、bandit（安全扫描）、vue-tsc（前端类型检查） |

安装方式：

```bash
pip install pre-commit && pre-commit install
```

### lint-staged（前端增量检查）

配置在 `frontend/package.json`，配合 Husky 在提交时自动对暂存文件执行 ESLint：

```json
{
  "lint-staged": {
    "*.{vue,ts,tsx}": ["eslint --fix"]
  }
}
```

手动执行：`npx lint-staged`

### vitest.config.ts — 测试配置

```typescript
export default defineConfig({
  test: {
    hookTimeout: 10000,      // 钩子超时（组件挂载/卸载等异步操作）
    testTimeout: 15000,      // 单个测试超时
    environment: 'jsdom',    // 模拟浏览器环境
    coverage: {
      provider: 'v8',
      reporter: ['text', 'json', 'html'],
    },
  },
})
```

---

## 开发约定

> ⚠️ 这些是团队总结的"踩坑经验"，写代码前务必了解。

### 核心约定（v1.4.1）

| 约定 | 通俗解释 |
|------|---------|
| **Service 序列化** | service 的 `_to_dict` 方法必须包含前端表格需要的所有字段（包括关联名称如 `village_name`），否则前端表格显示空白 |
| **async 调用必须 await** | 调用异步方法忘了写 `await`，代码不会报错但结果会是空的 |
| **Pydantic 默认值** | 列表/字典默认值必须用 `Field(default_factory=list/dict)`，直接写 `= []` 会导致多个对象共享同一个列表（经典 bug） |
| **AuditLog 字段映射** | `AuditLogService.log(resource, details, ip_address)` 对应模型列 `resource_type` / `metadata_` / `user_ip`，名字不一样别搞混 |
| **Windows libmagic** | `file_upload.py` 不能在顶层 `import magic`（Windows 会崩溃），用 stdlib `mimetypes` 代替 |
| **测试环境变量** | 不要用 `patch.dict(os.environ, clear=False)`（Windows 有长度限制），用 `monkeypatch.setenv` |
| **路由-service 契约** | 新增方法前先确认参数顺序和返回值格式，service 返回 dict 时路由不能再调 `.model_dump()` |
| **批量删除格式** | 批量删除接口接受 `payload: dict` 提取 `ids`，兼容前端 `{ids: [...]}` |

### UI 美化约定（2026-07-18）

| 约定 | 通俗解释 |
|------|---------|
| **样式 Token 强制** | 颜色只能用变量（`var(--xxx)` 或 `$xxx`），禁止写死十六进制值如 `#409eff` |
| **tokens-vars.scss 只放变量** | 这个文件里禁止写任何 CSS 规则（:root、选择器都不行），实体规则只在 `tokens.scss` 定义 |
| **错误状态必须显示** | 请求失败不能静默吞掉：要弹 ElMessage.error + 显示错误占位（el-result + 重试按钮） |
| **空数据用 el-empty** | 表格没数据时用 `#empty` 插槽显示 `<el-empty description="暂无数据" />` |
| **可点卡片无障碍** | 可点击的 div 必须加 `role="button" tabindex="0"` + 键盘事件；不可点的别加 `cursor:pointer` |
| **Element 蓝清理** | `#409eff` 是旧主题残留，一律改成 `var(--color-primary)` 系列 |

### 系统约定（2026-08-01）

| 约定 | 通俗解释 |
|------|---------|
| **ErrorBoundary 单一根** | `<transition>` 内的子元素必须有且仅有一个根元素。如需 `v-if`/`v-else`，用 `display:contents` 的包裹 div 实现。否则 Vue patch 算法会调用 `setAttribute('0')` 导致白屏崩溃 |
| **角色精简与归一化** | 系统只有 4 个角色：`super_admin`/`admin`/`user`/`viewer`。旧角色值通过 `normalize_role()` 自动映射，不需要手动迁移 |
| **前后端菜单对齐** | 前端 `menu-config.ts` 菜单 key 必须存在于后端 `menus.py` 的 `MENU_DEFINITIONS`，path 必须存在于前端路由。CI 用 `scripts/check_menu_alignment.py` 强制校验（已知废弃 key 白名单） |
| **字段命名契约** | 后端响应统一 snake_case，前端 `src/utils/camelize.ts` 提供 `camelizeDeep()/snakeizeDeep()` 双向转换工具（信封元数据键不转换），新代码优先 camelCase 消费 |
| **通行码三级回退** | Windows 下 `wmic` 生成的机器码不稳定，`machine_code_service.py` 已实现三级回退：精确匹配 → 模糊匹配 → 仅凭通行码匹配 |
| **API 响应格式统一** | 所有 API 端点返回 `{code:200, success:true, data:{...}, message:"成功"}` 信封格式。用 `success_response()`/`ok_list()`，禁止返回裸 dict。`camel_to_snake.py` 中间件对裸 dict 响应自动补全 `code/success/message` 元数据（不改变数据层级，兼容旧客户端与测试），文件/流式/错误响应除外 |
| **备份目标可配置** | 备份默认存应用数据目录；通过 `backup_target_dir` 配置可指向 U 盘/共享盘（防本机损坏）。`/system/backup/dirs` 检测磁盘、`/system/backup/target` 持久化 |
| **提醒写入消息表** | 提醒类型（approval_overtime/deadline_warning/budget_warning）写入 Message 表，用 `type:entity_id` 的 link 幂等去重，每 6 小时自动扫描 |
| **敏感操作二次确认** | 删除组织等敏感操作必须带 `confirm_password`（当前用户密码），防共用电脑误操作 |
| **增量数据包** | 数据包导出支持 `incremental` + `since_sync_version`，仅导出 `sync_version` 大于基准的记录，减小跨机传输体积 |

---

## 版本历史

| 版本 | 日期 | 主要变更 |
|------|------|---------|
| **v1.8.1（本文档）** | 2026-08-13 | 残余缺陷审计修复：报表 Word/PDF 导出真实实现（原返回空文件，现 python-docx/reportlab 真实渲染 + 经费/项目数据按年度真实聚合，PDF 内置中文字体）、PDF 导出路由补管理员权限校验（与 Word 一致）、断裂死方法清理、调试日志走统一 logger；后端 12163 用例全过；文档版本头全面更新、删除误入仓库的旧会话记录 |
| **v1.8.0** | 2026-08-12 | 20 项问题全面修复：经费全流程化（8 阶段步骤条/审批流程可视化/普通用户全量操作权限）、帮扶村年度数据与变更历史修复、政策文件预览、审批驳回必填原因、数据分析年度对比、贵州 88 县区补全、系统配置删除、消息中心扩展（备份提醒/30 天清理/托盘角标联动）、主题色 token 化（#409eff 清零）、资金周期/决算结算项目选择页、备份入口唯一、死代码清理；前端 351 文件 6362 测试全过、eslint/vue-tsc/flake8 零警告 |
| **v1.7.2** | 2026-08-09 | 备份包上传恢复完整链路（加密备份支持/流式落盘防OOM/is_encrypted 列表字段/verify 加密提示）、黑名单实现统一（删除异步死代码）、ENCRYPTION_KEY 自动配置、Electron 启动健康检查等待、fund_budgets/policy 覆盖率 100%、后端 12109 + 前端 6120 测试全过（覆盖率 99.86%） |
| **v1.7.1** | 2026-08-02 | 问题全量修复：组织删除二次确认前端适配、退出登录彻底清除记住登录、补 /dashboard/kpi-trends 与 yearly-trends 端点（分析页/大屏图表）、清理 332 个 pyc 仓库垃圾、恢复 30 个覆盖测试、reminders 权限限制、CI 修复（npm ci 跨平台 prepare / import audit 孤儿引号 / npm audit 0 漏洞 / 覆盖率阈值科学化 99-100%）| 全部 5 个 CI job 通过 |
| **v1.7.0** | 2026-08-02 | 单机版功能完善 3 期：数据安全与自愈（智能备份中心/磁盘检测/自检/自动打包/即时备份）、桌面体验（托盘角标/自启/快捷键/主题/成效大屏）、业务深化（增量数据包/提醒中心/驻村工作台/考核闭环/二次确认/自动锁屏）；后端 11577 + 前端 3239+ 测试全过 |
| **v1.6.0** | 2026-08-02 | 前后端 100% 对齐：响应信封统一（中间件自动补全 60 文件裸返回）、菜单 key 双向对齐（后端补 16 个、CI 门禁）、角色体系收敛（4 角色清理 12 处残留）、字段命名契约工具（camelize.ts）；同步全部测试断言，后端 11678 + 前端 3239 全过
| **v1.4.3** | 2026-08-01 | 新增系统约定（ErrorBoundary 单一根元素、角色精简与归一化、通行码三级回退、API 响应格式统一）；更新版本日期
| v1.5.0 | 2026-07-29 | 文档重写为小白友好版：新增架构概念介绍、技术栈简介、文件类型说明、快速查找指南；按功能区域重新组织内容；添加通俗解释和 emoji 标识 |
| v1.3.0 | 2026-07-25 | 文档结构再同步（与代码库对齐）：新增 backend/scripts、schemas、utils、interfaces 章节；api/v1 修订为 45 路由 + 5 子包；core 增至 47 模块；models 修订为 60 文件 / 118 类；services 修订为 98 + 8 子包；前端 stores 19 个、composables 31 个、views 31 目录 |
| 未发版 | 2026-07-18 | 前端 UI 美化：token 断层修复、additionalData 拆分、Element 蓝清理、error 态补全、a11y 增强、侧边栏响应式 |
| v1.4.1 | 2026-06-20 | 生产崩溃修复：多个 service 方法补全、async await 修复、字段映射修复、工作列表不显示数据根因修复、测试健壮性改进 |
| v1.4.0 | 2026-06 | RBAC 批量权限、权限撤销、treeNormalizer、E2E 冒烟测试、PR 门禁 CI、64-bit 迁移、pre-commit hooks、统一版本管理 |
| v1.2.0 | 2026-06 | 系统更名"帮扶管理信息系统"、经费字段对齐、PyInstaller 修复、100% 测试通过率、Alembic 基线合并 |
| v1.1.0 | 2026-04 | 菜单权限管理、工作台自定义布局、QuickActions、NSIS 安装包 |
| v1.0.4 | 2026-04 | 初始发布：帮扶村/项目/经费/学校核心功能 |

---

> 💡 **第一次接触本项目？建议阅读顺序：**
> 1. 本文件（了解整体结构）
> 2. `README.md`（快速上手）
> 3. `docs/01-快速开始/`（搭建开发环境）
> 4. `AGENTS.md`（构建命令 + 常见陷阱）
> 5. 根据你要改的功能，去对应目录看代码
