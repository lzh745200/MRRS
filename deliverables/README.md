# deliverables 目录索引

> 本目录存放对外交付物。**2026-09-14 清理**：历史会话报告归档、旧版 PPT 备份、
> 旧版校验和与历次门禁日志已删除（均可从 git 历史恢复），目录只保留"当前有效交付物"。

## 当前有效文档（v1.12.8 · 2026-09-14）

| 文档 | 说明 |
|---|---|
| `遗留风险彻底解决方案计划-2026-09-12.md` | R1–R14 风险清单：根因 / 严重度 / 解决步骤 / 责任分工 / 验证标准 / 复查机制 + **执行进度表**（当前治理主计划） |
| `archify/架构评估报告.md` | 架构评估结论（P0/P1/P2 整改依据） |
| `archify/system-map-runtime.html`、`archify/preview.png` | 运行时架构图（交互式 + 静态预览） |
| `software-company/hardening-r26-architecture.md` | 加固批次（R26）架构说明 |

## 演示材料（单一来源）

| 文件 | 说明 |
|---|---|
| `../docs/帮扶管理信息系统介绍.pptx` | **唯一的对外演示 PPT**（37 页，v1.12.8）——由 `docs/build_ppt.js` 生成，改内容后重跑 `node docs/build_ppt.js` 即可 |

> 旧版三份 PPT（`帮扶管理系统介绍.pptx` 等）与其 09-12 备份已删除：多份手工维护的
> 演示稿容易互相漂移，现收敛为"脚本生成 + 一处存放"。

## 安装包与校验和

| 项 | 获取方式 |
|---|---|
| Windows x64 安装包 | GitHub → Releases（推送 `v*` 标签由 `build-windows.yml` 构建并附带 `latest.yml`/blockmap） |
| 麒麟 ARM64 deb 包 | 同上（`build-arm64.yml`） |

> `SHA256SUMS-1.10.0.txt` 等旧版校验和已删除——校验和随每次 Release 一起发布，避免仓库内
> 留有过期副本（历史上就出现过"仓库里的校验和与最新安装包不一致"的误读）。

## 质量证据存放约定

- **不再在仓库内堆测试日志**：门禁结论以 GitHub Actions 运行记录 + Release 说明为准；
  README 的"项目状态"表维护当前基线数字（后端 11,244 用例 / 覆盖率 100%，前端 6,051 用例）。
- 需要临时留档时，请在会话结束前把结论**摘要**写进 CHANGELOG 或本目录文档，而不是提交原始日志。
