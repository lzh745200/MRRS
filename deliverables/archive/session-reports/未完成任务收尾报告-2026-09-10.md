# 未完成任务收尾报告（R23 · 版本 1.12.1）

- 日期：2026-09-10
- 范围：会话摘要中遗留任务的收尾 + 本仓 R22 全量残留失败清零
- 结论：**已完成；后端 10734 passed / 0 failed，前端全量绿，版本全仓统一 1.12.1**

---

## 一、背景与现状校正

会话摘要描述的"三维度优化 / 后端数据库诊断 / 前端缺陷修复"任务，其**已完成的
P0/P1 修复与实际代码库均已入库**（CHANGELOG 1.12.0 已含 R22 的 2FA、事务信息泄露、
外键 500 等）。收尾时实测确认：

- 摘要提到的 `docs/系统功能三维度优化方案.md`、`docs/后端数据库诊断修复报告.md`
  实际并不存在；对应的**在途成果**落在 `deliverables/系统完善优化建议方案-2026-09-10.md`
  与 `.scratch/w14-deep-probe/` 各轮工单中。
- 仓库已推进到 **w14-deep-probe R22/R23**：真实"未完成"是 R22 全量套件残留的 **3 例失败**，
  以及产品侧反馈的**帮扶村新建后列表不刷新**。

---

## 二、本轮修复的两处根因缺陷

### 缺陷 1：认证出口在「直接调用」路径崩溃（潜在生产 500）

- **现象**：R22 全量套件 3 例失败，均报
  `AttributeError: 'Depends' object has no attribute 'query'`（`app/core/security.py:322`）。
- **根因**：P1-2 性能优化把 `get_current_user` 由"自建 `SessionLocal()`"改为
  `db: Session = Depends(get_db)` 注入会话后，**直接调用方**不触发依赖注入，
  `db` 退化为 `Depends(get_db)` 哨兵对象。
- **真实影响（测试之外）**：`app/api/v1/system/backup.py::_jwt_user_from_request`
  同样直调 `get_current_user(...)` → **走 Bearer JWT 的备份接口（非 Electron 内部密钥
  通道）会 500**。此前未被测试捕获，因备份用例多走 `dependency_overrides` 或内部密钥。
- **修复**（`backend/app/core/security.py`）：`_owns_session = not isinstance(db, Session)`；
  未注入则自建会话、`finally` 关闭；注入则复用且不关闭（生命周期归 `get_db`，避免误关共享连接）。
- **回归**：新增 `tests/unit/test_get_current_user_session_contract_r23.py`
  （3 例，含备份生产路径直调断言）。

### 缺陷 2：帮扶村「新建成功却看不到新记录」

- **现象**：新建提交 → 前端提示成功并重置到第 1 页 → 列表无新行。
- **根因**：`GET /supported-villages` 固定 `order_by(SupportedVillage.id)` **升序**，
  新记录 id 最大、落在**最后一页**；且端点**未声明** `sort_by`/`sort_order`，
  前端部门列 `sortable="custom"` 下发的排序参数被 FastAPI 静默丢弃。
- **修复**（`backend/app/api/v1/supported_village.py`）：
  1. 新增 `sort_by`/`sort_order` 查询参数；
  2. `_SORTABLE_COLUMNS` 白名单（camelCase prop 与 snake_case 列名都登记，
     杜绝 `getattr` 越界到非列属性）；
  3. `_resolve_village_sort()` 默认 `id` 倒序（最新在前），非法列安全回退；
  4. 缓存 key 纳入排序维度。与 `projects.py`（默认 `Project.id.desc()`）约定对齐。
- **回归**：新增 `tests/unit/test_supported_village_sort_r23.py`（4 例）。

---

## 三、此前反馈的两项前端问题核对

| 反馈 | 现状 | 结论 |
|------|------|------|
| 工作台"机器码管理"按钮点击无响应 | `dashboard/index.vue` 的 `isAdmin` 已含 `super_admin`/`is_superuser`；`pushSafe('/admin/machine-code')` 路由存在 | 已在 HEAD 修复 |
| 左侧导航缺"机器码管理" | `DefaultLayoutSafe.vue` 系统管理子菜单已含"机器码与通行码"（`/admin/machine-code/management`，`authStore.isAdmin` 门控）；`menu-config.ts` 亦有 `machine-code` 项 | 已在 HEAD 存在 |
| 帮扶村新建后列表不刷新 | 后端默认升序 → 新记录落末页 | **本轮修复** |

---

## 四、验证矩阵

| 项 | 命令 | 结果 |
|----|------|------|
| 后端全量 | `pytest tests/ -q --timeout=90` | **10734 passed / 0 failed**（1113s，1 warning） |
| 后端静态 | `flake8 app/ --max-line-length=120`（改动文件） | 0 |
| 前端类型 | `vue-tsc --noEmit` | 0 |
| 前端单测 | `vitest run` | 全绿（详见 `.scratch/w14-deep-probe/r23-frontend2.txt`） |
| 版本一致 | `node scripts/sync-version.js --check` | 通过 ✅（全仓 1.12.1） |

> 说明：后端全量在"帮扶村排序改动"落地前启动，故该改动由随后的
> `test_supported_village_*.py`（123 例）+ 新增排序回归（4 例）单独验证通过；
> 其余全部测试由全量套件覆盖。

---

## 五、并发会话协调（重要）

本工作区在收尾期间存在**并发会话**（见 `.workbuddy/memory/2026-09-10.md`）。
该会话已完成：完成度盘点、3 处阻断点修复（`ReportExport.vue` 缺 `printTime`、
`exportUtil.ts` 全角空格、版本号 8 处漂移经 `scripts/sync-version.js` 同步），
并经 `r23_probe.py` 产出审批流 E2E 探针结果。其**明确不提交**，将提交职责留给本会话。
本会话据此统一提交，避免双写竞态。

---

## 六、开放项（本轮不夹带，记录备查）

1. **审批流探针 R23.9/R23.10（3 例）**：`POST /funds/{id}/reject` 无驳回原因返回
   400「驳回原因不能为空」（属**设计校验**，可能为探针预期偏差）；审批任务列表
   `n=0` 与"驳回后任务仍 pending"疑为数据范围（org scope）按设计过滤，待产品确认。
2. **R22 审计启发式残余 34 条**：15 bare `db.commit()`、7 缺 `write_work_log`、
   12 `filter_by_data_scope` 提示——多为 admin/system 端点或设计选择，留待专门轮次逐条判定。
3. **Backlog**：`004-backlog-deferred-features.md`（5 项）、
   `005-two-factor-session-hardening.md`（3 项）、
   `deliverables/系统完善优化建议方案-2026-09-10.md`（P1–P3）。
