# 架构师诊断报告 — 帮扶管理系统项目治理

> **项目路径**：`C:\military-Rural Revitalization-system`
> **技术栈**：Vue3 + Vite 前端 / Python + FastAPI 后端 / Docker 部署 / PyInstaller 打包 / Electron 桌面端
> **分析方式**：只读扫描（ls / find / git ls-files / grep），未修改任何文件
> **扫描范围**：排除 node_modules/、.venv/、.venv-arm64/、.git/、dist/、build/、__pycache__/、.mypy_cache/、.pytest_cache/、artifacts/frontend-dist/ 的内部内容（但判断其是否应被 .gitignore 覆盖）
> **Git 跟踪文件总数**：1615

---

## 一、问题诊断报告

### 1.1 代码异味（Code Smells）

| 编号 | 问题描述 | 位置 | 严重程度 | 处理建议 |
|------|---------|------|---------|---------|
| CS-01 | 嵌套空目录 `backend/backend/`，内含误生成的 `nul` 文件和空的 `secrets/`，疑似构建脚本路径错误导致 | `backend/backend/` | P1 | 直接删除整个 `backend/backend/` 目录（未被 git 跟踪，无源码） |
| CS-02 | `backend/MagicMock/` 目录下含 `mock/` 和字面量目录名 `settings.UPLOAD_DIR/`，是单元测试 mock 对象泄漏到文件系统 | `backend/MagicMock/` | P1 | 直接删除（已被 .gitignore 覆盖，未跟踪）；排查测试代码中误创建目录的逻辑 |
| CS-03 | 6 个 PyInstaller .spec 文件功能重叠：`assistance-management-backend.spec` 与 `-x86.spec` 几乎相同（均 console=False、同名），`bumofu-arm64.spec` 使用不同 exe 名 `bumofu-server` 命名不一致 | `backend/*.spec` | P1 | 合并为 2-3 个 spec（x64/arm64/standalone），统一 exe 名称，移除冗余 |
| CS-04 | 8 个构建脚本散落在根目录（build.ps1/build.sh/build-kylin.sh/build-arm64-local.ps1/build-with-check.ps1/clean.ps1/fix-build.ps1/run.ps1），同时 `build-scripts/` 目录又有一套独立构建脚本 | 根目录 + `build-scripts/` | P1 | 统一归入 `build-scripts/`，根目录仅保留入口脚本（如 `Makefile` 已存在） |
| CS-05 | 6 个 requirements 文件 + `.backup` + `.tmp`，依赖管理碎片化；主 `requirements.txt` 混入开发/测试工具（pytest/black/flake8/mypy/pylint/pyinstaller 等），且有重复条目 `packaging==26.0`（第 155 行与第 177 行） | `backend/requirements*.txt` | P1 | 主 requirements.txt 仅保留运行时依赖，开发工具移入 requirements-dev.txt；删除重复行；评估 6 个 requirements 文件能否精简为 2 个（prod/dev） |
| CS-06 | `backend/app/.pyc_compiled` 为 0 字节垃圾文件且被 git 跟踪 | `backend/app/.pyc_compiled` | P2 | 从 git 移除并删除文件（.gitignore 已含 `*.pyc_compiled` 但此无扩展名文件漏网） |
| CS-07 | `backend/app/services/` 目录跟踪 120 个文件，模块体积过大，可能缺乏子模块拆分 | `backend/app/services/` | P2 | 评估按业务域拆分子目录（如 ai/、approval/、auth/ 等） |
| CS-08 | 7 个 AI 工具配置目录散落根目录（.agents/.arts/.claude/.context/.gstack/.workbuddy/claude-code），其中 `.agents/`(4文件) 和 `.context/`(1文件) 被 git 跟踪 | 根目录 | P2 | 统一归入 `.tools/` 或全部加入 .gitignore；`.agents/`、`.context/` 应停止跟踪 |
| CS-09 | 测试批次清单文件 `batch_am.txt`、`batch_nz.txt`（内容为测试文件路径列表）被跟踪入版本库 | `backend/batch_am.txt`、`backend/batch_nz.txt` | P2 | 从 git 移除，加入 .gitignore |
| CS-10 | `backend/tests/` 跟踪 272 个文件，含 `COVERAGE_100_ROADMAP.md` 等过程文档混入测试目录 | `backend/tests/` | P2 | 过程文档移至 docs/，测试目录仅保留测试代码 |
| CS-11 | 根目录 `nul`、`backend/nul`、`backend/app/nul` 三个 Windows 重定向误创建文件 | 多处 | P2 | 删除（根 nul 已 gitignore；backend/nul 与 backend/app/nul 未跟踪） |

### 1.2 冗余文件

| 编号 | 问题描述 | 位置 | 严重程度 | 处理建议 |
|------|---------|------|---------|---------|
| RD-01 | 构建日志 `build.log`（241KB）残留根目录 | 根目录 `build.log` | P2 | 直接删除（已 gitignore） |
| RD-02 | 对话记录文件 2 个：`conversation-2026-06-19-102506.txt`（162KB）、`backend/conversation-2026-06-12-213340.txt`（209KB） | 根目录 + `backend/` | P2 | 直接删除（已 gitignore） |
| RD-03 | CSRF cookie 文件 2 个：`csrf_cookies.txt`、`csrf_cookies3.txt` | 根目录 | P2 | 直接删除（已 gitignore） |
| RD-04 | 测试数据库 3 个：`test.db`(4KB)、`test_encrypted_package.db`(2.5MB)、`token_blacklist.db`(16KB) 放在源码目录 | `backend/` | P1 | 直接删除（已 gitignore `*.db`）；测试应使用临时目录或 fixtures |
| RD-05 | 17 个 `uploads_snapshot_*` 快照目录残留 | `backend/uploads_snapshot_*` | P2 | 直接删除（已 gitignore） |
| RD-06 | 覆盖率文件 `.coverage` 2 个（根目录 147KB + backend 139KB） | 根目录 + `backend/` | P2 | 直接删除（已 gitignore） |
| RD-07 | `requirements.txt.backup` 被跟踪入版本库 | `backend/requirements.txt.backup` | P1 | 从 git 移除并删除（应使用 git 历史而非备份文件） |
| RD-08 | `requirements.txt.tmp` 临时文件 | `backend/requirements.txt.tmp` | P2 | 直接删除（已 gitignore） |
| RD-09 | `frontend/test_results.txt` 测试结果被跟踪 | `frontend/test_results.txt` | P1 | 从 git 移除并删除 |
| RD-10 | `frontend/vitest.json` 0 字节空文件被跟踪 | `frontend/vitest.json` | P2 | 从 git 移除并删除 |
| RD-11 | 空的构建产物目录：`archives/`、`artifacts/`(仅含 frontend-dist)、`backups/`、`dist/`、`build/`（根级与 backend 级） | 多处 | P2 | 保留目录但确保 .gitignore 覆盖（已覆盖）；可清理实际产物 |
| RD-12 | `frontend/uploads/`、`frontend/data/` 运行时数据目录误放在前端目录 | `frontend/` | P1 | 移至 backend 运行时目录或删除（运行时数据不应在前端） |
| RD-13 | `frontend/coverage/`、`frontend/dist/` 构建产物 | `frontend/` | P2 | 清理产物（已 gitignore coverage/，dist/ 由根级 `dist/` 覆盖需确认） |
| RD-14 | `.venv-arm64/` 第二虚拟环境（ARM64 交叉编译用） | `backend/` | P2 | 已 gitignore；建议用容器构建替代本地双 venv |
| RD-15 | 根目录 `nul`（0字节）、`backend/nul`（0字节）Windows 误创建文件 | 多处 | P2 | 直接删除 |

### 1.3 未使用依赖

> 验证方法：在 `backend/app/`、`backend/scripts/`、`backend/tests/`（后端）和 `frontend/src/`（前端）中 grep 实际 import 语句。.venv 内的匹配不计入。

**后端未使用依赖（主 requirements.txt）：**

| 编号 | 包名 | 版本 | 验证结果 | 严重程度 | 处理建议 |
|------|------|------|---------|---------|---------|
| UD-01 | Scrapy | 2.11.1 | app 中 0 处 import | P1 | 移除（连带其依赖 Twisted/hyperlink/Automat/Protego/queuelib 等可一并移除） |
| UD-02 | Twisted | 25.5.0 | 0 处 import（Scrapy 的依赖） | P1 | 随 Scrapy 移除 |
| UD-03 | hyperlink / Automat / constantly / Incremental / zope.interface | — | 0 处 import（Twisted 依赖链） | P1 | 随 Twisted 移除 |
| UD-04 | feedparser | 6.0.11 | 0 处 import | P1 | 移除 |
| UD-05 | tldextract | 5.3.1 | 0 处 import | P1 | 移除 |
| UD-06 | snownlp | 0.12.3 | 0 处 import | P1 | 移除 |
| UD-07 | plotly | 6.6.0 | 0 处 import | P1 | 移除 |
| UD-08 | structlog | 25.5.0 | 0 处 import（项目用 loguru） | P1 | 移除 |
| UD-09 | opentelemetry-api/sdk/instrumentation-*/exporter-otlp | 1.40.0 | 0 处 import | P1 | 移除（4 个包） |
| UD-10 | prometheus_client | 0.24.1 | 0 处 import（app 手动生成 prometheus 格式文本，未用库） | P1 | 移除 |
| UD-11 | slowapi | 0.1.9 | 0 处实际 import（仅注释提及，代码用自建内存限流器） | P2 | 移除或确认是否计划启用 |
| UD-12 | diskcache | 5.6.3 | 0 处 import | P1 | 移除 |
| UD-13 | pypng | 0.20220715.0 | 0 处 import | P2 | 移除 |
| UD-14 | fpdf2 | 2.8.7 | 0 处 import | P1 | 移除 |
| UD-15 | mammoth | 1.6.0 | 0 处 import | P1 | 移除 |
| UD-16 | cssselect | 1.4.0 | 0 处 import | P2 | 移除 |
| UD-17 | pdf2image | 1.16.3 | 0 处 import | P1 | 移除 |
| UD-18 | scipy / scikit-learn | — | 0 处直接 import（仅作为 fontTools/matplotlib 间接依赖存在于 .venv） | P2 | 可从主 requirements 移除显式声明，让传递依赖处理 |

> **注意**：`prophet`（在 `trend_prediction_service.py` 中 try/except 可选导入）和 `matplotlib`（在 `utils/chart.py` 中可选导入）**确认在用**，保留。`cmdstanpy`/`stanio` 为 prophet 传递依赖，可保留但不必显式声明。

**前端未使用依赖（package.json）：**

| 编号 | 包名 | 验证结果 | 严重程度 | 处理建议 |
|------|------|---------|---------|---------|
| UD-19 | leaflet | 仅 Help.vue 文字描述提及，0 处实际 import | P1 | 移除（或确认地图功能是否计划启用） |
| UD-20 | lodash-es | 0 处 import | P1 | 移除 |
| UD-21 | nprogress | 0 处 import（grep 命中的是组件名 ProgressChart/ProgressDialog，非 nprogress 包） | P1 | 移除 |
| UD-22 | mammoth | 仅 env.d.ts 类型声明，0 处实际 import | P2 | 移除或确认是否计划启用 |
| UD-23 | pretty-format (devDep) | 0 处引用 | P2 | 移除 |

### 1.4 安全隐患

| 编号 | 问题描述 | 位置 | 严重程度 | 处理建议 |
|------|---------|------|---------|---------|
| SE-01 | `docker-compose.yml` 含弱默认密码：`POSTGRES_PASSWORD=${DB_PASSWORD:-assistance123}`、`SECRET_KEY=${SECRET_KEY:-change-me-in-production}`，生产环境若未设置环境变量将使用弱凭据 | `docker-compose.yml` 第 37、95 行 | P1 | 移除默认值，强制要求显式设置；或使用生成器在启动时创建随机密码 |
| SE-02 | `backend/secrets/master.key`（44 字节）存在于工作目录 | `backend/secrets/` | P2 | 已被 .gitignore 覆盖（未跟踪）✓；建议改用环境变量或 KMS 管理，而非文件 |
| SE-03 | `.agents/`（4 文件）和 `.context/`（1 文件）AI 工具工作目录被 git 跟踪 | `.agents/`、`.context/` | P2 | 停止跟踪（git rm --cached），加入 .gitignore，避免工具内部状态/可能的 prompt 泄露入库 |
| SE-04 | 根目录 `csrf_cookies.txt`、`csrf_cookies3.txt` 残留 cookie 文件 | 根目录 | P2 | 已 gitignore（未跟踪）✓；直接删除文件 |
| SE-05 | 多处 `.env` 文件含 SECRET_KEY/CSRF_SECRET_KEY 等敏感配置 | `.env`、`backend/.env`、`frontend/.env` | — | 已确认全部未被 git 跟踪 ✓（.gitignore 覆盖完善）；保持现状 |
| SE-06 | 4 个根级 Dockerfile（Dockerfile/Dockerfile.arm64/Dockerfile.fpm/Dockerfile.runtime）+ backend/Dockerfile + frontend/Dockerfile，镜像构建入口分散，维护成本高 | 根目录 + backend + frontend | P2 | 整合为统一的 docker/ 目录管理，减少 Dockerfile 数量 |

> **安全结论**：`.env`、`secrets/`、`*.db`、`*.key`、`*.pem` 均已正确 gitignore 且未跟踪，无密钥泄露入库。主要风险为 docker-compose 弱默认密码和 AI 工具目录误跟踪。

---

## 二、目标目录结构设计

```
military-Rural-Revitalization-system/
├── backend/                          # 后端源码（Python/FastAPI）
│   ├── app/                          # 应用主代码
│   │   ├── api/                      # 路由层（90 文件）
│   │   ├── core/                     # 核心配置/安全/依赖（44 文件）
│   │   ├── models/                   # 数据模型（59 文件）
│   │   ├── schemas/                  # Pydantic 模型（26 文件）
│   │   ├── services/                 # 业务服务（120 文件，建议按域拆子目录）
│   │   ├── middleware/               # 中间件（14 文件）
│   │   ├── interfaces/               # 接口定义（5 文件）
│   │   ├── utils/                    # 工具函数（32 文件）
│   │   ├── data/                     # 应用级静态数据
│   │   ├── main.py                   # 应用入口
│   │   ├── config.py                 # 配置
│   │   └── dependencies.py           # 依赖注入
│   ├── alembic/                      # 数据库迁移
│   ├── tests/                        # 测试代码（仅 .py，过程文档移走）
│   ├── scripts/                      # 后端脚本
│   ├── requirements.txt              # 仅运行时依赖
│   ├── requirements-dev.txt          # 开发/测试工具
│   ├── pyproject.toml                # 项目配置
│   └── pytest.ini
├── frontend/                         # 前端源码（Vue3/Vite）
│   ├── src/                          # 源码
│   ├── public/                       # 静态资源
│   ├── tests/                        # 单元测试
│   ├── e2e/                          # E2E 测试
│   ├── package.json
│   ├── vite.config.ts
│   └── tsconfig.json
├── electron/                         # Electron 桌面端（6 文件，保留）
├── docker/                           # 部署相关（统一归口）
│   ├── Dockerfile.backend            # 后端镜像
│   ├── Dockerfile.frontend           # 前端镜像
│   ├── Dockerfile.runtime            # 运行时镜像
│   ├── docker-compose.yml
│   └── nginx/
├── build-scripts/                    # 构建脚本统一目录（含 PyInstaller spec、NSIS 安装包）
│   ├── specs/                        # PyInstaller .spec 文件（精简为 2-3 个）
│   ├── installers/                   # NSIS 安装脚本
│   └── build-config.json
├── deploy/                           # 部署配置（k8s/kylin）
├── docs/                             # 文档统一目录（已存在，39 文件）
├── scripts/                          # 项目级脚本（dev-setup/cleanup/start-all）
├── resources/                        # 公共资源
├── data/                             # 运行时数据（gitignore）
├── logs/                             # 运行时日志（gitignore）
├── uploads/                          # 上传文件（gitignore，仅 backend 保留）
├── .tools/                           # AI 工具配置统一目录（.agents/.context 等归入）
├── .github/                          # CI/CD
├── .gitignore
├── .env.example                      # 环境变量模板（唯一入库的 .env*）
├── Makefile                          # 统一任务入口
├── README.md
├── CHANGELOG.md
├── CONTRIBUTING.md
└── LICENSE
```

**设计原则说明：**
1. **源码 / 构建产物 / 文档 / 部署 / 测试 严格分离** — 后端构建产物不入 backend/，前端构建产物不入 frontend/
2. **构建脚本统一归口** `build-scripts/`，根目录仅保留 `Makefile` 作为入口
3. **Dockerfile 归入** `docker/` 目录，减少根目录噪音
4. **AI 工具目录统一** `.tools/`，避免 7 个点目录散落
5. **运行时数据**（data/logs/uploads）统一 gitignore，不入版本库
6. **根目录文档精简** — 仅保留 README/CHANGELOG/CONTRIBUTING/LICENSE，其余移入 docs/

---

## 三、清理执行清单

> 按风险从低到高排序。高风险操作标注"需备份后执行"。

| 步骤 | 操作类型 | 源路径 | 目标路径 | 风险等级 | 备注 |
|------|---------|--------|---------|---------|------|
| 1 | 删除 | `build.log` | — | 低 | 241KB 构建日志，已 gitignore |
| 2 | 删除 | `conversation-2026-06-19-102506.txt` | — | 低 | 162KB 对话记录，已 gitignore |
| 3 | 删除 | `backend/conversation-2026-06-12-213340.txt` | — | 低 | 209KB 对话记录，已 gitignore |
| 4 | 删除 | `csrf_cookies.txt`、`csrf_cookies3.txt` | — | 低 | cookie 文件，已 gitignore |
| 5 | 删除 | `nul`（根）、`backend/nul`、`backend/app/nul` | — | 低 | Windows 误创建文件 |
| 6 | 删除 | `.coverage`（根）、`backend/.coverage` | — | 低 | 覆盖率文件，已 gitignore |
| 7 | 删除 | `backend/test.db`、`backend/test_encrypted_package.db`、`backend/token_blacklist.db` | — | 低 | 测试数据库，已 gitignore |
| 8 | 删除 | `backend/uploads_snapshot_*`（17 个目录） | — | 低 | 快照目录，已 gitignore |
| 9 | 删除 | `backend/requirements.txt.tmp` | — | 低 | 临时文件，已 gitignore |
| 10 | 删除 | `backend/MagicMock/` | — | 低 | 测试 mock 泄漏，已 gitignore |
| 11 | 删除 | `backend/backend/` | — | 低 | 误建嵌套目录，未跟踪 |
| 12 | git rm --cached + 删除 | `backend/app/.pyc_compiled` | — | 低 | 0 字节垃圾文件，已跟踪 |
| 13 | git rm --cached + 删除 | `frontend/test_results.txt` | — | 低 | 测试结果，不应入库 |
| 14 | git rm --cached + 删除 | `frontend/vitest.json` | — | 低 | 0 字节空文件 |
| 15 | git rm --cached + 删除 | `backend/batch_am.txt`、`backend/batch_nz.txt` | — | 低 | 测试批次清单，不应入库 |
| 16 | git rm --cached + 删除 | `backend/requirements.txt.backup` | — | 低 | 备份文件，用 git 历史替代 |
| 17 | git rm --cached | `.agents/`、`.context/` | — | 低 | AI 工具目录，停止跟踪 |
| 18 | 删除 | `frontend/uploads/`、`frontend/data/` | — | 中 | 运行时数据误放前端；确认无引用后删除 |
| 19 | 移动 | 根级 `Dockerfile`、`Dockerfile.arm64`、`Dockerfile.fpm`、`Dockerfile.runtime`、`docker-compose.yml`、`nginx/` | `docker/` | 中 | 需同步更新 CI/CD 和构建脚本中的路径引用 |
| 20 | 移动 | 根级 `build.ps1`、`build.sh`、`build-kylin.sh`、`build-arm64-local.ps1`、`build-with-check.ps1`、`clean.ps1`、`fix-build.ps1` | `build-scripts/` | 中 | 需更新 Makefile 和 CI 引用 |
| 21 | 移动 | `backend/*.spec`（6 个） | `build-scripts/specs/` | 中 | 需更新 spec 内相对路径 |
| 22 | 合并 | `assistance-management-backend.spec` + `-x86.spec` | `build-scripts/specs/backend-x64.spec` | 中 | 两者几乎相同，合并为单一 x64 spec |
| 23 | 移动 | `.agents/`、`.context/`、`.claude/`、`.arts/`、`.gstack/`、`.workbuddy/`、`claude-code/` | `.tools/` | 中 | 需更新各工具配置路径；**需备份后执行** |
| 24 | 编辑 | `backend/requirements.txt` | — | 中 | 移除 18 个未使用包 + 重复 packaging 行；开发工具移入 requirements-dev.txt；**需备份后执行** |
| 25 | 编辑 | `frontend/package.json` | — | 中 | 移除 leaflet/lodash-es/nprogress/mammoth/pretty-format；**需备份后执行** |
| 26 | 编辑 | `docker-compose.yml` | — | 中 | 移除弱默认密码（assistance123/change-me-in-production） |
| 27 | 移动 | `backend/tests/COVERAGE_100_ROADMAP.md` 等过程文档 | `docs/` | 低 | 测试目录仅保留测试代码 |
| 28 | 编辑 | `.gitignore` | — | 低 | 补充缺失条目（见第四节） |

---

## 四、.gitignore 建议补充项

当前 `.gitignore` 已较为完善（165 行），覆盖了 .env、*.db、secrets/、构建产物、.venv 等。以下为建议补充：

```gitignore
# ===== AI 工具目录（当前 .agents/ .context/ 仍被跟踪）=====
.agents/
.context/
# 注：.claude/ .arts/ .gstack/ .workbuddy/ claude-code/ 已覆盖

# ===== PyInstaller 临时构建产物 =====
backend/build/
backend/dist/
*.spec.bak

# ===== Electron 构建产物 =====
electron/dist/
electron/release/

# ===== 前端额外产物 =====
frontend/.vite/
frontend/stats.html

# ===== 根级误创建文件防范 =====
/nul
/backend/nul
/backend/app/nul

# ===== 测试相关临时文件 =====
backend/batch_*.txt
*.test-results.*
frontend/test_results.txt
frontend/vitest.json

# ===== requirements 备份 =====
**/requirements.txt.backup
**/requirements.txt.tmp

# ===== .pyc_compiled 无扩展名垃圾 =====
.pyc_compiled
**/.pyc_compiled

# ===== OS 特定 =====
ehthumbs.db
*.lnk
```

---

## 附：扫描统计摘要

| 维度 | 数量 |
|------|------|
| Git 跟踪文件总数 | 1615 |
| 后端未使用依赖包 | 18 个（含 Scrapy 全家桶、OpenTelemetry 全家桶等） |
| 前端未使用依赖包 | 5 个 |
| 冗余文件（可直接删除） | 30+ 个文件 / 17 个快照目录 |
| 误跟踪应移除文件 | 7 个（.pyc_compiled / test_results.txt / vitest.json / batch_*.txt / requirements.txt.backup / .agents / .context） |
| 散落的构建脚本 | 根级 8 个 + build-scripts/ 一套 |
| PyInstaller spec 文件 | 6 个（可精简为 2-3 个） |
| requirements 文件 | 6 个 + .backup + .tmp |
| AI 工具配置目录 | 7 个 |
| 安全风险项 | 1 个 P1（docker-compose 弱默认密码） |
```
