# 帮扶管理信息系统 · 计划完成度检测报告

| 项目 | 内容 |
|------|------|
| 检测日期 | 2026-09-10 |
| 检测范围 | 全部计划文档 / 工单 / 路线图 / 优化方案 / 探针记录 |
| 代码基线 | `86182deb`（HEAD）+ 工作树在途 v1.12.1 |
| 检测方式 | 计划文档盘点 + 工单状态核对 + 前后端全量门禁实测 + 版本一致性校验 |
| 总体结论 | **系统处于健康、接近可发布状态**；发现并修复 3 类阻断点；仍有 8 项 backlog 待立项 |

---

## 一、执行摘要（TL;DR）

1. **健康面**：后端全量 `pytest` **10731 通过 / 0 失败**；`flake8` 0；前端 `vue-tsc` 0、`eslint` 0、全量 `vitest` **6021 通过**；`alembic` 单 head 且库已在 head；全仓版本号统一 **1.12.1**。
2. **修复面**：检测中发现 3 类阻断点（前端 2 处 + 版本漂移 1 类），已全部修复并复验转绿。
3. **未完成面**：8 项计划内功能仍处 backlog/deferred（详见第四节），另有多项优化建议（P1–P3）待排期。
4. **重要事实**：检测期间确认**另一并发会话正在实时收尾 v1.12.1**（R23 探针进行中），本会话仅做检测与阻断点修复，未提交未推送，避免竞态。

---

## 二、已交付并入库（✅ committed）

| 领域 | 交付内容 | 证据 |
|------|----------|------|
| 治理波次 | w1 安全红线 ~ w13 文档发布，共 50+ 工单 | `.scratch/w1…w13/` |
| 深度探针 | w14-deep-probe R1–R22 逐模块真实 HTTP 探针与修复 | `.scratch/w14-deep-probe/001/002` |
| 安全 | 2FA 两处高危（中间态令牌越权、恢复码可复用）、事务层 SQL 泄露、8 端点外键 500、机器码多用户注册 | `f4f7ced6` / `86182deb` 等 |
| 业务缺陷 | 政策附件多文件丢失、合同附件覆盖备注、注册 refresh 契约、帮扶村年度板块路由顺序、经费上传 category 丢失 | R1/R9/R14/R15/R21 |
| 功能补齐 | 报表订阅闭环（工单 003）、E2E 150 用例纳入 CI | `dbe91630` / `28aeb85b` |
| 质量 | 前后端覆盖率 100%、UI 全面优化（1.12.0） | CHANGELOG `[1.12.0]` |
| 文档 | 需求规格说明书、系统设计文档全集、API 文档、数据隔离审计矩阵 | `docs/` |

---

## 三、已完成但未提交（🔧 v1.12.1 在途）

> 工作树存在约 108 项未提交改动，均为 v1.12.1 一个连贯工作流；**并发会话正在实时编辑**。

| 变更 | 说明 |
|------|------|
| R23 认证会话契约 + 帮扶村列表排序 | `get_current_user` 直调路径崩溃（潜在 500）+ 新建村列表不可见（默认改 id 倒序） |
| P1-2 性能优化 | `get_current_user` 复用 `Depends(get_db)` 注入会话（每请求 1 个 DB 连接） |
| 权限收敛 E1/E2 | 删除 `user_permissions` / `unified_data_scope` / `data_scope_adapter` / `village/` 包、`OperationLogs.vue`、UserManagement 冗余菜单权限对话框 |
| 报表导出美化 | 新增 `app/utils/excel_report_style.py`、`pdf_report_style.py`；`ReportExport.vue` 打印页眉页脚；`exportUtil.ts` PDF 样式升级 |
| DB 健康 | 新增 `app/services/database_health_service.py`（含 WAL checkpoint 相关） |

---

## 四、未完成 / 待排期（❌ open）

### 4.1 计划内未完成功能（`004-backlog-deferred-features.md`，5 项）

| # | 功能 | 现状 | 风险 | 前置 |
|---|------|------|------|------|
| 1 | SSE 实时消息推送 | 前端轮询已工作，后端无 SSE 端点 | 与 token 认证集成、单进程多连接开销 | 产品确认推送事件集合 |
| 2 | Decimal 全链路化（Pydantic 层） | 金额仅应用层 `quantize_money` | **高**——波及前端契约与全部测试断言 | 需独立回归波次 |
| 3 | 备份审批流 | 备份仅管理员，无审批链 | 中——审批状态机审计敏感 | 产品确认需审批的操作 |
| 4 | 数据归档导出 | auto_package + 手动导出已覆盖主场景 | 低-中 | 产品确认归档对象 |
| 5 | 前端 MoneyText 统一组件 | 金额格式化逻辑散落各视图 | 低，量大机械 | 无 |

### 4.2 2FA 会话强度加固（`005-two-factor-session-hardening.md`，3 项）

| # | 项 | 严重度 | 说明 |
|---|----|--------|------|
| 1 | `/two-factor/enable`、`/disable` 不要求当前密码 | 中（纵深防御缺口） | 敏感操作缺再认证，属**契约变更**需协调 e2e |
| 2 | 2FA 登录失败不计入账户锁定 | 低 | 可复用既有 `locked_until` 逻辑，需产品确认阈值 |
| 3 | `/two-factor/status` 不暴露恢复码余量 | 无安全影响 | 可用性——余量 ≤2 应提示 |

### 4.3 完善优化建议（`deliverables/系统完善优化建议方案-2026-09-10.md`）

| 优先级 | 项 | 状态 |
|--------|----|------|
| P1-1 | 中间件链精简与条件化（14+ 层） | ❌ 未做 |
| P1-2 | 消除「每请求双 DB 连接」 | 🔧 v1.12.1 已做 |
| P1-3 | WAL 每小时 checkpoint + 容量告警 | 🔧 部分（`database_health_service`） |
| P1-4 | 死代码与类型契约清理 | 🔧 部分（策略选项已清） |
| P2-1 | 索引精简与数据库瘦身（506 索引/127 表） | ❌ |
| P2-2 | 附件体系完善（去重/孤立清理/统一校验） | ❌ |
| P2-3 | 慢请求追踪与可观测性 | ❌ |
| P2-4 | 启动时间优化（~8s） | ❌ |
| P3-1~5 | Decimal 化、备份审批流、数据归档、包体积优化、检索增强 | ❌ |

### 4.4 需求路线图（`docs/系统完整需求规划文档.md §4`）

- 第二优先级：Decimal 全链路化、备份审批流 —— 未完成
- 第三优先级：数据归档导出、前端 MoneyText 统一组件 —— 未完成

---

## 五、本次检测中发现并修复的阻断点（🛠）

| # | 文件 | 问题 | 修复 |
|---|------|------|------|
| 1 | `frontend/src/views/export/ReportExport.vue` | 新增打印页眉页脚时漏声明 `printTime` ref → `vue-tsc` TS2339/TS2304 | 补 `const printTime = ref('')` |
| 2 | `frontend/src/utils/exportUtil.ts:106` | PDF 元信息串含全角空格 U+3000 → `eslint no-irregular-whitespace` | 改为 ` · ` 分隔 |
| 3 | 8 个版本文件 | 权威源已升 1.12.1 但未跑完整同步（`version.txt`/`README.md`/`docker-compose.yml`/`scripts/docker/build.ps1`/`constants.ts`/`version.json`/`Dockerfile.kylin-standalone`/`electron/main.js`）→ 前端 `constants.test.ts` 2 例失败 | `node scripts/sync-version.js --write`（8 文件，校验通过） |

---

## 六、健康检查实测结果

| 门禁 | 命令 | 结果 |
|------|------|------|
| 后端全量测试 | `pytest tests/` | **10731 passed / 0 failed**（18:56，1 warning） |
| 后端 lint | `flake8 app/ --max-line-length=120` | **0** |
| 后端导入自检 | `python -c "import app.main"` | OK（46 业务模块加载） |
| 前后端定向回归 | 核心改动模块相关测试 | 239（后端）+ 158（前端）全通过 |
| 前端类型检查 | `vue-tsc --noEmit` | **0** |
| 前端 lint | `eslint src --max-warnings=0` | **0** |
| 前端全量测试 | `vitest run` | **299/300 文件、6019/6021 用例**（另 2 例为版本漂移致，已修复转绿） |
| 数据库迁移 | `alembic heads` / `current` | 单 head `policy_attachments_001`，库已在 head |
| 版本一致性 | `node scripts/sync-version.js --check` | **EXIT=0**（全仓 1.12.1） |

---

## 七、风险与下一步建议

1. **⚠️ 并发会话**：检测期间确认另一会话正实时收尾 v1.12.1（`.scratch/w14-deep-probe/006-r23-*.md`、`r23_probe.py`、`r23-run/`、`r23-fullsuite.txt` 等持续新增）。**本会话未提交/未推送**，避免与并发会话竞态。
2. **建议由 v1.12.1 收尾方统一提交**：将 R23 + P1-2 + 权限收敛 + 报表美化 + 版本同步作为一个 v1.12.1 提交并推送、触发双安装包构建。
3. **发布前仍建议人工执行一次真机 UI 安装抽验**（此前记录：真机抽验为待人工项）。
4. **backlog 立项**：SSE、Decimal、备份审批流、数据归档、MoneyText 五项已评估范围/风险/前置，可按业务优先级排期。
5. **性能优化**：P1-1（中间件精简）、P1-3（WAL checkpoint 完整化）为低风险高收益项，建议优先推进。

---

*本报告由主理人基于实测数据编制，计划来源包括 `.zcode/plans/`、`.scratch/w14-deep-probe/`、`docs/`、`deliverables/` 与 CHANGELOG。*
