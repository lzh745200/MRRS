# 清理执行日志 — 帮扶管理系统

> **执行人**：工程师寇豆码（software-engineer）
> **执行日期**：2026-06-22
> **Baseline commit**：`9726cf3`（fix(backend): 修复日志格式化崩溃类BUG 5项）
> **执行依据**：`deliverables/架构师诊断报告.md` 第三节清理执行清单（28 步）
> **关键调整**：步骤 23 不物理移动 AI 工具目录（保持原位，仅 .gitignore 覆盖）

---

## 阶段 A：安全网（已完成）

| 操作 | 结果 |
|------|------|
| `git status` | 工作区干净，仅 untracked `deliverables/` 目录，无未提交改动 |
| `git stash` | 跳过（无需暂存） |
| Baseline commit | `9726cf3` |
| 执行日志文件 | 已创建（本文件） |

---

## 阶段 B：低风险删除（步骤 1-17，全部成功）

| 步骤 | 操作 | 结果 |
|------|------|------|
| 1 | 删除 `build.log`（241KB） | ✅ 成功 |
| 2 | 删除 `conversation-2026-06-19-102506.txt`（162KB） | ✅ 成功 |
| 3 | 删除 `backend/conversation-2026-06-12-213340.txt`（209KB） | ✅ 成功 |
| 4 | 删除 `csrf_cookies.txt`、`csrf_cookies3.txt` | ✅ 成功 |
| 5 | 删除 `nul`（根）、`backend/nul`、`backend/app/nul` | ✅ 成功 |
| 6 | 删除 `.coverage`（根）、`backend/.coverage` | ✅ 成功 |
| 7 | 删除 `backend/test.db`、`backend/test_encrypted_package.db`、`backend/token_blacklist.db` | ✅ 成功 |
| 8 | 删除 `backend/uploads_snapshot_*`（18 个目录，报告称 17 个，实际 18 个） | ✅ 成功 |
| 9 | 删除 `backend/requirements.txt.tmp` | ✅ 成功 |
| 10 | 删除 `backend/MagicMock/` | ✅ 成功 |
| 11 | 删除 `backend/backend/`（嵌套空目录） | ✅ 成功 |
| 12 | `git rm --cached` + 删除 `backend/app/.pyc_compiled` | ✅ 成功 |
| 13 | `git rm --cached` + 删除 `frontend/test_results.txt` | ✅ 成功 |
| 14 | `git rm --cached` + 删除 `frontend/vitest.json` | ✅ 成功 |
| 15 | `git rm --cached` + 删除 `backend/batch_am.txt`、`backend/batch_nz.txt` | ✅ 成功 |
| 16 | `git rm --cached` + 删除 `backend/requirements.txt.backup` | ✅ 成功 |
| 17 | `git rm -r --cached .agents/ .context/`（5 文件停止跟踪，物理文件保留） | ✅ 成功 |

---

## 阶段 C：中风险移动（步骤 18-22）

| 步骤 | 操作 | 结果 |
|------|------|------|
| 18 | 删除 `frontend/uploads/`（空目录）、`frontend/data/`（运行时 db+cache，未跟踪、无源码引用） | ✅ 成功 |
| 19 | 移动 4 个根级 Dockerfile → `docker/`（`git mv` 保留历史）；更新 5 处引用（docker-compose.yml、build.ps1、build-with-check.ps1、build-kylin.sh×2） | ✅ 部分成功 |
| 19-保留 | `docker-compose.yml` 和 `nginx/` 保留原位 | ⚠️ 保留：docker-compose.yml 含 ~10 处根目录相对路径（./data、./logs、./nginx、./config、./backups 等）和 `context: .`，移动需改全部为 `../`，风险过高 |
| 20 | 根级构建脚本 → `build-scripts/` | ⚠️ 保留：脚本间交叉引用（run.ps1→build.ps1、fix-build.ps1→build.ps1、build.ps1→run.ps1），且 run.ps1 不在移动清单中，移动会造成断裂；Makefile 已提供统一入口 |
| 21 | `backend/*.spec` → `build-scripts/specs/` | ⚠️ 保留：CI workflow（build-windows.yml）和 3 个 batch 脚本按名称从 backend/ 目录引用 spec 文件，spec 内含相对路径（../app 等），移动会断裂构建 |
| 22 | 合并 spec 文件 | ⚠️ 保留：依赖步骤 21，一并保留 |
| 23 | AI 工具目录 → `.tools/` | ⚠️ 按主理人调整跳过：.claude/.workbuddy 等正在使用中，物理移动会破坏工具；已通过步骤 17（git rm --cached）+ 步骤 28（.gitignore 补充）处理 |

---

## 阶段 D：配置编辑（步骤 24-26, 28）

### 步骤 24：精简 requirements.txt ✅

**移除的未使用包（14 项，grep 双保险验证 0 import）：**
- Scrapy 全家桶：Scrapy、parsel、w3lib、itemloaders、itemadapter、Protego、queuelib、PyDispatcher、requests-file、sgmllib3k（10 个）
- Twisted 依赖链：Twisted、hyperlink、Automat、constantly、Incremental、zope.interface（6 个）
- 其他：feedparser、tldextract、snownlp、plotly、structlog、opentelemetry-api/sdk/instrumentation-fastapi/instrumentation-sqlalchemy/exporter-otlp（5+4=9 个）、slowapi、pypng、fpdf2、cssselect、pdf2image（12 个）
- 重复行：`packaging==26.0`（第二处移除，保留第一处）

**⚠️ 关键发现 — grep 双保险拦截 4 个误删包（架构师报告遗漏 try/except 内可选导入）：**
| 包名 | 架构师判定 | 实际 grep 结果 | 处理 |
|------|-----------|---------------|------|
| scikit-learn | UD-18 未使用 | 4 处 import（ai_service.py、trend_prediction_service.py、anomaly_detection_service.py×2） | ✅ 保留 |
| prometheus_client | UD-10 未使用 | 2 处 import（system_metrics.py、db_metrics.py） | ✅ 保留 |
| diskcache | UD-12 未使用 | 2 处 import（dashboard.py、map.py） | ✅ 保留 |
| mammoth | UD-15 未使用 | 1 处 import（policy.py:913） | ✅ 保留 |

**保留确认：** prophet、matplotlib、cmdstanpy、stanio、scipy、scikit-learn、prometheus_client、diskcache、mammoth 全部保留

**开发工具迁移：** pytest/black/flake8/mypy/pylint/pyinstaller 等 34 个开发/测试工具包移至 `requirements-dev.txt`（原文件损坏含 JS 代码，已覆盖重建为正确内容）

### 步骤 25：精简 package.json ✅

- 移除 dependencies：leaflet、lodash-es、mammoth、nprogress（4 个，grep 验证 0 import）
- 移除 devDependencies：pretty-format、@types/leaflet、@types/lodash-es、@types/nprogress（4 个，随主包移除）
- 保留核心依赖：vue、vite、element-plus、pinia、vue-router、echarts、axios 等全部完好

### 步骤 26：docker-compose.yml 弱密码修复 ✅

- `SECRET_KEY=${SECRET_KEY:-change-me-in-production}` → `SECRET_KEY=${SECRET_KEY:?必须设置 SECRET_KEY 环境变量}`
- `POSTGRES_PASSWORD=${DB_PASSWORD:-assistance123}` → `POSTGRES_PASSWORD=${DB_PASSWORD:?必须设置 DB_PASSWORD 环境变量}`
- 效果：未设置环境变量时容器启动将报错退出，杜绝弱凭据

### 步骤 28：.gitignore 补充 ✅

追加 20 条缺失项：.agents/、.context/、backend/dist/、*.spec.bak、electron/dist/、electron/release/、frontend/.vite/、frontend/stats.html、/nul、/backend/nul、/backend/app/nul、backend/batch_*.txt、*.test-results.*、frontend/test_results.txt、frontend/vitest.json、**/requirements.txt.backup、**/requirements.txt.tmp、.pyc_compiled、**/.pyc_compiled、ehthumbs.db、*.lnk

---

## 阶段 E：自检验证（全部通过）

### 1. git status 改动汇总

| 类型 | 数量 | 明细 |
|------|------|------|
| 删除跟踪（D） | 11 文件 | .agents/(4) + .context/(1) + .pyc_compiled + batch_am/nz.txt + requirements.txt.backup + test_results.txt + vitest.json |
| 重命名（R） | 4 文件 | 4 个 Dockerfile → docker/ |
| 修改（M） | 8 文件 | .gitignore、requirements.txt、requirements-dev.txt、package.json、docker-compose.yml、build.ps1、build-with-check.ps1、build-kylin.sh |
| 物理删除（未跟踪） | 30+ 文件 | 日志/cookie/db/snapshot/MagicMock/nul 等临时文件 |

### 2. 关键引用验证

| 检查项 | 结果 |
|--------|------|
| 根级 Dockerfile 引用 | ✅ 全部更新为 `docker/Dockerfile`，无残留旧引用 |
| docker/ 目录 4 个 Dockerfile | ✅ 全部存在 |
| requirements.txt 含 prophet/matplotlib | ✅ 确认（2 处） |
| package.json 核心依赖 | ✅ vue/vite/element-plus/pinia/vue-router 全部保留（6 匹配） |
| package.json 已移除依赖 | ✅ 0 处残留（leaflet/lodash-es/nprogress/pretty-format） |
| docker-compose.yml 弱密码 | ✅ 0 处残留（assistance123/change-me-in-production） |

### 3. 基础导入验证

| 验证项 | 命令 | 结果 |
|--------|------|------|
| 后端导入 | `.venv/Scripts/python.exe -c "import app.main"` | ✅ 成功（42/42 路由加载，爬虫服务离线模式正常） |
| 前端类型检查 | `npx vue-tsc --noEmit` | ✅ 成功（0 错误） |

> **后端验证说明**：爬虫服务日志显示"爬虫服务已禁用（离线模式）"，这是移除 Scrapy 后的预期行为——代码中 `ENABLE_CRAWLER` 默认 false，爬虫功能通过 try/except 可选导入，无 Scrapy 时自动降级为离线模式，不影响系统运行。

---

## 执行摘要

### 步骤执行统计

| 状态 | 步骤数 | 步骤号 |
|------|--------|--------|
| ✅ 成功执行 | 20 | 1-18, 24, 25, 26, 28 |
| ⚠️ 部分执行 | 1 | 19（仅移动 Dockerfile，docker-compose.yml/nginx 保留） |
| ⚠️ 按主理人调整跳过 | 1 | 23（不物理移动 AI 工具目录） |
| ⚠️ 保留原位（风险过高） | 3 | 20, 21, 22 |
| 未分配（不在执行流程中） | 1 | 27 |
| **合计** | 28 | |

### 改动文件统计

- **删除跟踪**：11 个文件（git rm --cached / git rm）
- **物理删除**：30+ 个文件/18 个目录（临时文件、日志、db、snapshot 等）
- **重命名移动**：4 个文件（Dockerfile → docker/）
- **编辑修改**：8 个文件（配置文件）
- **新建/覆盖**：1 个文件（requirements-dev.txt 覆盖重建）

### 保留未执行项及原因

| 步骤 | 保留原因 | 建议 |
|------|---------|------|
| 19（docker-compose.yml + nginx） | 含 ~10 处根目录相对路径和 `context: .`，移动需全改为 `../`，风险过高 | 可在后续重构中配合 docker-compose.override.yml 逐步迁移 |
| 20（构建脚本） | 脚本间交叉引用 + run.ps1 不在清单中，移动造成断裂；Makefile 已提供统一入口 | 如需移动，建议将 run.ps1 一并纳入清单并更新交叉引用 |
| 21-22（spec 文件） | CI workflow + 3 个 batch 脚本按名称从 backend/ 引用，spec 内相对路径会断裂 | 如需移动，需同步更新 CI + batch 脚本 + 调整 spec 内 pathex/datas 相对路径 |
| 23（AI 工具目录） | 按主理人决策不物理移动（.claude/.workbuddy 正在使用） | 已通过 .gitignore 覆盖，无需物理移动 |
| 27（测试文档移动） | 不在主理人分配的执行流程中 | 可在后续文档整理阶段处理 |

### 需要 QA 验证的重点

1. **Docker 构建**：验证 `docker build -f docker/Dockerfile .` 和 `docker-compose build` 能正常工作（Dockerfile 路径已更新）
2. **后端依赖安装**：在干净环境中 `pip install -r backend/requirements.txt` 验证无缺失依赖
3. **前端构建**：`cd frontend && npm install && npm run build` 验证移除依赖后构建正常
4. **PyInstaller 打包**：验证 spec 文件仍能从 backend/ 目录正常执行（spec 未移动）
5. **docker-compose 启动**：验证需显式设置 DB_PASSWORD 和 SECRET_KEY 环境变量后才能启动
