# 数据库设计总览

> **版本**:v1.10.0  **更新日期**:2026-08-29
> **定位**:用"图"看懂数据库——全库 9 大业务域的关系鸟瞰 + 每个域的实体关系图(ER 图)+ 核心业务实体精讲。
> **适用读者**:想理解"数据是怎么组织的"的任何人。先读本文建立整体图景,再按需查阅《02-数据字典》逐表细节。
> **图示说明**:所有图都是 **Mermaid** 画法。看图诀窍:方框 = 表/实体;连线上的记号 `||--o{` 读作"一(‖)对多(o{)";如 `users ||--o{ funds` = 一个用户可以有多条经费。

---

## 0. 一句话总结

> 整个数据库围绕 5 个"主角"展开:**用户(User)** 在 **组织(Organization)** 里工作,结对帮扶 **村庄(SupportedVillage)** 和 **学校(School)**,开展 **项目(Project)**、管理 **经费(Fund)**;所有重要变更走 **审批**,单位之间靠 **数据包** 交换数据;其余 100 多张表都是这 7 类主角的"档案明细"。

## 1. 全域鸟瞰图

**怎么看这张图**:9 个大格子是 9 个业务域,箭头表示"域之间谁引用谁"。`用户与权限` 是地基(所有域都引用它),`帮扶对象` 和 `项目资金` 是业务主干,`数据上报` 是连接多台电脑的桥梁。

```mermaid
graph TB
    subgraph D1["① 用户与权限 (16表)"]
        U["users 用户<br/>organizations 组织树<br/>rbac_* 细粒度权限<br/>machine_codes 机器码授权"]
    end
    subgraph D2["② 帮扶对象 (30表)"]
        V["supported_villages 帮扶村<br/>+ 11张年度板块子表<br/>schools 学校 / villages 自然村"]
    end
    subgraph D3["③ 项目与资金 (24表)"]
        F["projects 项目<br/>funds 经费(8状态机)<br/>合同/凭证/决算/异常/预算"]
    end
    subgraph D4["④ 审批 (4表)"]
        A["approval_workflows 流程<br/>approval_tasks 待办"]
    end
    subgraph D5["⑤ 政策 (3表)"]
        P["policies 政策库"]
    end
    subgraph D6["⑥ 乡村工作 (4表)"]
        W["rural_works / rural_tasks<br/>work_logs 工作日志"]
    end
    subgraph D7["⑦ 数据上报 (8表)"]
        PK["data_packages 加密数据包<br/>data_reports 上报单"]
    end
    subgraph D8["⑧ 消息待办 (4表)"]
        M["messages / todos"]
    end
    subgraph D9["⑨ 系统运维 (20表)"]
        O["audit_logs 审计<br/>监控/告警/配置/校验规则"]
    end

    U -->|"organization_id / created_by<br/>几乎所有表都引用"| D2
    U --> D3
    D2 -->|"项目挂在村下"| D3
    D3 -->|"重要变更送审"| D4
    D2 -->|"变更送审"| D4
    D7 -->|"包导入即批量写业务数据"| D2
    D7 --> D3
    D2 --> W
    D9 -.->|"审计记录一切操作"| D1
```

## 2. 核心业务实体精讲(先看这张!)

**这 7 个实体撑起系统主干**,理解它们之间的关系,其他表都只是"它们的明细档案":

```mermaid
erDiagram
    organizations ||--o{ users : "雇用(单位有多个账号)"
    organizations ||--o{ organizations : "上下级(parent_id 自引用)"
    users ||--o{ supported_villages : "建档帮扶单位挂靠"
    supported_villages ||--o{ projects : "一个村多个帮扶项目"
    supported_villages ||--o{ village_population : "年度人口档案"
    supported_villages ||--o{ village_income : "年度收入档案"
    projects ||--o{ funds : "项目下的经费"
    supported_villages ||--o{ funds : "直达村的经费"
    schools ||--o{ funds : "直达校的经费"
    users ||--o{ funds : "申请人/审批人"
    funds ||--o{ fund_status_history : "状态流转史"
    funds ||--o{ fund_transactions : "使用明细"
    approval_workflows ||--o{ approval_tasks : "流程产生任务"
    approval_tasks }o--|| funds : "任务写回(如经费审批)"

    supported_villages {
        int id PK
        string village_name "村名"
        int organization_id FK "帮扶单位"
        string transition_status "过渡期状态"
    }
    projects {
        int id PK
        string name "项目名"
        int village_id FK "所属帮扶村"
        string status "项目状态"
    }
    funds {
        int id PK
        string name "经费名"
        int project_id FK "挂项目(可空)"
        int village_id FK "或直达村(可空)"
        int school_id FK "或直达校(可空)"
        string status "8态生命周期"
        numeric amount "金额"
    }
    users {
        int id PK
        string username
        string role "4种角色"
        string data_scope "数据范围4档"
        int organization_id FK
    }
```

**白话故事**:帮扶单位(organizations 里的一个节点)的干部(users)给结对村(supported_villages)建档,每年填人口、收入等"体检表"(village_population 等 11 张子表);村里要修路,就立一个项目(projects),项目要花钱就申请经费(funds)——经费可以是这个项目的,也可以不经过项目直接给村/校;钱每"走一步"(申请→批准→拨付→使用→完工→审计)都在 fund_status_history 里留一条记录,而审批环节由审批流水线(approval_*)驱动。

## 3. 分域 ER 图

### 3.1 用户与权限域(16 表)

**怎么看**:左半是"账号与组织",右半是"细粒度权限(rbac_*,UUID 主键)"和"离线授权(机器码)"。`user_organizations` 是一张纯粹的"配对表"(多对多中间表)。

```mermaid
erDiagram
    organizations ||--o{ users : "所属单位"
    organizations ||--o{ user_organizations : ""
    users ||--o{ user_organizations : "多单位身份"
    users ||--o{ two_factor_auth : "双因素(1:1)"
    users ||--o{ token_blacklist : "作废凭证"
    users ||--o{ user_roles : ""
    rbac_roles ||--o{ user_roles : "角色分配"
    rbac_roles ||--o{ role_permissions : "角色权限"
    users ||--o{ user_permissions : "个人直授"
    users ||--o{ resource_access : "单资源ACL"
    users ||--o{ machine_codes : "绑定机器"
    organizations ||--o{ machine_codes : "机器归属单位"
    machine_codes ||--o{ machine_code_permissions : "功能白名单"
    users ||--o{ permission_packs : "权限包(创建)"
    users {
        int id PK
        string role "super_admin|admin|user|viewer"
        string data_scope "all|org|org_children|self"
        int permission_pack_id FK
        int token_version "登出+1全失效"
    }
    machine_codes {
        int id PK
        string machine_code "硬件指纹"
        string status "pending|active|revoked"
        string pass_code "通行码"
    }
```

> **权限生效优先级**(记住这个就够):用户自定义菜单(allowed_menus)> 权限包(permission_packs)> 角色默认菜单。细粒度操作允许与否:用户直授 > 角色权限 > 拒绝。

### 3.2 帮扶对象域(30 表)

**怎么看**:中心是帮扶村;11 张年度子表结构完全同构(村+年唯一),图中合并画为"年度板块×11";右侧是与帮扶村**互不关联**的自然村家族(⚠️ 常见误解);下方是独立的学校线。

```mermaid
erDiagram
    supported_villages ||--o{ village_population : "年度人口"
    supported_villages ||--o{ village_income : "年度收入"
    supported_villages ||--o{ force_investment : "部队投入"
    supported_villages ||--o{ industry_support : "产业"
    supported_villages ||--o{ infrastructure_improvement : "基建"
    supported_villages ||--o{ party_building_support : "党建"
    supported_villages ||--o{ medical_support : "医疗"
    supported_villages ||--o{ consumption_support : "消费"
    supported_villages ||--o{ employment_support : "就业"
    supported_villages ||--o{ education_support : "教育"
    supported_villages ||--o{ support_funding : "过渡经费"
    supported_villages ||--o{ village_committee_info : "村委会"
    village_committee_info ||--o{ village_committee_members : "成员"
    supported_villages ||--o{ village_attachments : "附件"
    supported_villages ||--o{ annual_income : "新版年度统计×4"
    villages ||--o{ villagers : "村民"
    villages ||--o{ industries : "产业"
    villages ||--o{ tea_plantations : "茶园"
    villages ||--o{ cactus_fruit_plots : "刺梨园"
    schools ||--o{ school_attachments : "附件"
    schools ||--o{ school_supports : "帮扶流水"
    schools ||--o{ school_projects : "校项目"
    schools ||--o{ scholarship_students : "受助学生"
    users ||--o{ report_subscriptions : "报表订阅"
```

### 3.3 项目与资金域(24 表)

**怎么看**:项目是"事",经费是"钱"。经费 19 张配套表按功能分四簇:**使用**(台账)、**管控**(预算/基线/版本)、**流转**(凭证/合同/拨款)、**风控**(异常/决算/留痕)。

```mermaid
erDiagram
    supported_villages ||--o{ projects : "村里立项目"
    projects ||--o{ project_tasks : "任务"
    projects ||--o{ project_files : "附件"
    projects ||--o{ project_milestones : "里程碑"
    projects ||--o{ project_change_logs : "变更留痕"
    projects ||--o{ funds : "项目经费"
    supported_villages ||--o{ funds : "直达村经费"
    schools ||--o{ funds : "直达校经费"
    funds ||--o{ fund_status_history : "状态史"
    funds ||--o{ fund_field_changes : "字段改动史"
    funds ||--o{ fund_operation_logs : "操作日志"
    funds ||--o{ fund_attachments : "附件"
    funds ||--o{ fund_transactions : "使用台账"
    fund_budgets ||--o{ fund_transactions : "报销挂科目"
    supported_villages ||--o{ fund_budgets : "村级预算"
    funds ||--o{ budget_baselines : "预算基线快照"
    funds ||--o{ budget_versions : "预算改版"
    projects ||--o{ project_fund_phases : "7阶段推进"
    funds ||--o{ fund_transfer_vouchers : "划转凭证"
    funds ||--o{ fund_contracts : "合同"
    fund_contracts ||--o{ fund_contract_payments : "付款明细"
    project_tasks ||--o{ fund_contract_payments : "关联任务"
    funds ||--o{ fund_anomalies : "异常检测"
    projects ||--o{ fund_settlements : "决算"
    funds ||--o{ fund_allocation_orders : "拨款指令"
    fund_allocation_orders ||--o{ allocation_order_items : "拆分明细"
    projects ||--o{ fund_asset_verifications : "资产核验"
    fund_budgets {
        int id PK
        int village_id FK
        int year
        string category "8科目"
        numeric amount
    }
    fund_transactions {
        int id PK
        int fund_id FK
        numeric amount
        string status "pending|approved|rejected"
    }
```

### 3.4 审批域(4 表)

```mermaid
erDiagram
    approval_workflows ||--o{ approval_nodes : "各级审批人(≤5级)"
    approval_workflows ||--o{ approval_tasks : "产生任务"
    users ||--o{ approval_tasks : "提交人/当前审批人"
    approval_tasks ||--o{ approval_records : "签批留痕"
    users ||--o{ approval_records : "审批人/被转交人"
    approval_nodes {
        int id PK
        int workflow_id FK
        int level "第几级"
        string approver_type "user|role"
    }
    approval_tasks {
        int id PK
        string entity_type "哪个模块的变更"
        string status "pending|approved|rejected|withdrawn|approved_apply_failed"
        json original_data "改前快照"
        json change_data "改后快照"
    }
```

> **要点**:Task 里的 `original_data`/`change_data` 是页面"变更对比"的数据来源;`approved_apply_failed` 状态 = 审批通过但写回业务失败,支持重试。

### 3.5 政策域(3 表)/ 3.6 乡村工作域(4 表)

```mermaid
erDiagram
    policy_categories ||--o{ policy_categories : "分类树"
    policies }o--|| policy_categories : "归类"
    users ||--o{ policy_favorites : "收藏"
    policies ||--o{ policy_favorites : ""
    villages ||--o{ rural_works : "工作事项(自然村)"
    rural_works ||--o{ rural_tasks : "子任务"
    villages ||--o{ rural_tasks : ""
    users ||--o{ work_logs : "干部日志"
    supported_villages ||--o{ work_logs : ""
    supported_villages ||--o{ effectiveness_evaluations : "成效评估"
```

> 注意:乡村工作挂**自然村**(villages),成效评估挂**帮扶村**(supported_villages)——两个"村"各管一摊。

### 3.7 数据上报域(8 表)

```mermaid
erDiagram
    organizations ||--o{ data_packages : "谁家的包"
    users ||--o{ data_packages : "创建/导入人"
    data_packages ||--o{ data_reports : "包对应上报单"
    organizations ||--o{ data_reports : "报送方/接收方"
    data_packages ||--o{ package_versions : "版本"
    data_packages ||--o{ import_export_history : "操作审计"
    users ||--o{ import_histories : "Excel导入史"
    users ||--o{ export_tasks : "异步导出"
    users ||--o{ data_sync_logs : ".rrs增量同步"
    data_sync_logs ||--o{ data_conflicts : "冲突清单"
    data_packages {
        int id PK
        string type "task下发|report上报"
        string status "draft-pending-validated-imported"
        string checksum "SHA256"
        boolean is_encrypted "AES-256-GCM"
    }
```

### 3.8 消息待办(4 表)/ 3.9 系统运维(20 表)

```mermaid
erDiagram
    users ||--o{ messages : "站内消息"
    users ||--|| notification_preferences : "通知偏好(1:1)"
    users ||--o{ todos : "待办"
    users ||--o{ audit_logs : "操作审计"
    audit_logs ||--o{ audit_changes : "字段级明细"
    users ||--o{ security_events : "安全事件"
    alert_rules ||--o{ alert_history : "告警历史"
    users ||--o{ report_templates : "报表模板"
```

## 4. 命名陷阱与历史包袱(改动前必读)

| # | 陷阱 | 说明 |
|---|------|------|
| 1 | **`village_id` 实指帮扶村** | `projects.village_id`、`funds.village_id`、`work_logs.village_id`、`fund_budgets.village_id`、`fund_transactions.village_id` 的外键目标都是 `supported_villages.id`(列名是历史遗留),**不是** `villages.id` |
| 2 | **双"村庄宇宙"** | `villages`(自然村+村民+产业+茶园,地理字典)与 `supported_villages`(帮扶业务主档)**没有任何外键连接**。乡村工作/任务挂前者,帮扶业务挂后者 |
| 3 | **软删除双轨制** | 旧五大家族(SupportedVillage/School/Project/Fund/Policy)用 `is_active=False + deleted_at`;新模型用 `SoftDeleteMixin(is_deleted + deleted_at + deleted_by)`。写查询时先确认目标表用的是哪套 |
| 4 | **裸整数无外键** | `policies.organization_id`、`school.created_by`、`org_module_policies.organization_id`、`subordinate_instances.organization_id`、`rbac_access_logs.user_id`(还是 String(36)!)等字段是普通整数,**数据库不做引用校验**,删被引用行不会报错 |
| 5 | **RBAC 用 UUID 主键** | `rbac_*` 六件套主键是 String(36),与全库 Integer 主键风格不同;`ApprovalNode.approver_id` 按 `approver_type` 存"用户ID或角色ID"(多态,无法建外键) |
| 6 | **汇总冗余字段** | `supported_villages.transition_fund_*_total`、`funds.used_amount` 等是**计算后冗余存储**的汇总值——改动明细后必须同步更新主档,否则报表对不上 |
| 7 | **sync_version 是同步基石** | 增量数据包靠"每行版本号 > 上次同步点"识别变更;任何绕过 SQLAlchemy 直接 UPDATE 的写法都**不会**触发版本号自增,会导致该行永远不同步 |

## 5. 历史文档说明

旧版《[ER-DIAGRAM.md](../../ER-DIAGRAM.md)》(2026-05-31,54 模型)已由本目录两篇文档取代:旧图含已删除实体(UserSession/EffectivenessIndicator/PackageEditLog 等,它们在 2026-08 死代码清理中移除),且未覆盖 rbac/审计/数据包等域。该文件现仅作指路页保留,防止外部链接失效。

**数据结构以代码为准**:`backend/app/models/`(定义)+ `backend/alembic/versions/`(迁移链,基线见 `012_consolidate_baseline.py`)。本文与字典若与代码冲突,以代码为准并欢迎修正文档。
