FastAPI + Uvicorn ASGI 服务器；SQLAlchemy ORM 搭配 SQLite（WAL 模式）；Alembic 进行版本化数据库迁移；Pydantic 做数据校验；PyInstaller 打包为桌面可执行（含 Linux ARM64 spec）；pytest + coverage 要求 100% 覆盖率门禁；ruff/flake8/mypy/pylint 多工具代码质量检查。