Electron（`BrowserWindow`/`Tray`/`globalShortcut`/`safeStorage`/`ipcMain`/`ipcRenderer`）、Node `worker_threads` 实现任务池、PyInstaller onedir 布局的后端 `assistance-backend` 可执行文件、AES-256-GCM + scrypt 文件级加解密、SQLite (`rural_revitalization.db`)。