后端可执行文件路径需兼容两种 PyInstaller 布局：`resources/backend/assistance-backend/assistance-backend(.exe)`（onedir）与旧版 `resources/backend/assistance-backend(.exe)`（onefile）；首次冷启动因 PyInstaller 解包+杀软扫描可能超过 30 秒，健康检查超时设为 `BACKEND_READY_TIMEOUT=300000`。