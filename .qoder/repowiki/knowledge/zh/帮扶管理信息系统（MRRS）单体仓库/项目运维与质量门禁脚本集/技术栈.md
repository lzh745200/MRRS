Python 3 + pathlib/re/subprocess 标准库；Node.js/npm 用于前端依赖；Windows PowerShell (`build.ps1`/`run.ps1`) 与 Bash (`dev-setup.sh`、`verify_deb_smoke.sh`) 双端 shell；Git hooks 通过 `pre-commit` 框架集成 flake8、bandit、vue-tsc。