- 所有新 API 统一返回 core/response.py 中的 success_response / ok_list envelope 格式
- 业务查询必须经 filter_by_data_scope / apply_scope_filter 按组织范围过滤，禁止绕过数据隔离
- 写操作通过 write_work_log() 记录审计日志，审批/授权/密码类变更强制留痕
- 核心模型采用 is_active + deleted_by 软删除，审计记录永不物理删除
- 敏感信息（如身份证号）落地加密，日志与错误响应不得泄露内部细节
- 构建产物版本号统一来自根 package.json.version，并通过 --build-arg VERSION 注入 Docker 与打包流程